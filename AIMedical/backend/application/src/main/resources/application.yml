spring:
  application:
    name: aimedical-sys
  profiles:
    active: phase1,dev

server:
  port: 8080

# JWT配置
jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: 900
  refresh-token-expiration: 604800
