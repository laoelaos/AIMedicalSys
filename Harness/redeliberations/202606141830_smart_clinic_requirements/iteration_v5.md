# 再审议判定报告（v5）

## 判定结果

RETRY

## 判定理由

组件B诊断报告（b_v5_diag_v1.md）共识别出 **1 项严重问题**与 **4 项一般问题**。组件B质询报告（b_v5_challenge_v1.md）以 LOCATED 结论确认诊断：证据充分、逻辑自洽、维度覆盖到位，10 项问题定位准确；仅在问题 2（6.7 等待提示时长口径）发现一处论证表述瑕疵（误读 "12s/6s/8s" 为等待提示时长本身，实际为硬超时值），但质询明确该瑕疵属"论证表述偏差"而非"问题判定错误"，按改进建议落地即可消解实质不一致，不影响报告整体可信度。

根据判定标准"审查报告包含严重或一般等级的问题"应判定为 RETRY。本诊断报告含 1 项严重问题（8.1 范围内 AI 能力描述与 1.4 / 3.4.x 严重不一致，直接影响下游范围与验收判定）以及 4 项一般问题（6.7 与 3.4.3 修订后硬超时的同步缺口、AI 错误码命名约定 8/13 与 5/13 的二分分裂、3.1.3.2 业务-契约归属缺失、6.3 降级路径仅覆盖 5/13），且组件B内部循环仅完成 1 轮（最大 12 轮），尚有迭代空间，应触发重新运行组件A。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：第 8.1 节"范围内"采用 v1 时代"三大 AI 模块 + 等能力"表述，与 1.4 节"13 项独立 AI 能力"清单及 3.4.1—3.4.13 完整契约存在 6—10 倍量级脱节；AI 开立检查/检验（3.4.9）、AI 辅助开方（3.4.10）、AI 执行顺序推荐（3.4.11）、AI 医生排班（3.4.12）、AI 综合讨论结论（3.4.13）等 5 项在 8.1 节被完全隐没。
- **所在位置**：第 8.1 节"范围内"（line 1471）
- **严重程度**：严重
- **改进建议**：将 8.1 节 AI 能力描述重写为与 1.4 / 3.4.x 一致的 13 项完整清单（或至少明示"共 13 项独立 AI 能力，详见 1.4 / 3.4.x"），删除"三大 AI 模块"与"等能力"模糊措辞；同步在 8.2 节确认 13 项 AI 能力均在范围内、不再属于范围外候选。

- **问题描述**：6.7 节"AI 调用超时与重试"示例值（智能诊断 12s、处方审核 6s、病历生成 8s）与所声明的"硬超时 + 5s 网络缓冲"公式口径混淆；其中"病历生成 8s"直接对应 3.4.3 的 P95（≤ 8 秒）而非 v5 第 2 轮修订后的非流式硬超时（≤ 12 秒），下游前端按 6.7 字面设定等待提示将与 3.4.3 实际硬超时出现系统级偏差。
- **所在位置**：6.7 节"AI 调用超时与重试"（line 1393–1395）；3.4.3 服务质量要求（line 804–811）
- **严重程度**：一般
- **改进建议**：将 6.7 示例改为以 3.4.x 实际硬超时为基线，明示"等待提示时长 = 硬超时 + 5s 网络缓冲"；按质询反馈区分"真实矛盾（病历生成 8s 应改为 12s）"与"已正确项（智能诊断 12s、处方审核 6s 与 3.4.x 硬超时一致）"，仅修正真实不一致。

- **问题描述**：3.4.x 共 13 个 AI 能力错误码命名分为两派：8 个带 `_AI` 后缀（3.4.1/3.4.4/3.4.5/3.4.6/3.4.7/3.4.8/3.4.12/3.4.13），5 个不带（3.4.2/3.4.3/3.4.9/3.4.10/3.4.11）；3.4.1 内部也存在 `TRIAGE_AI_TIMEOUT/UNAVAILABLE` 与 `TRIAGE_INPUT_INVALID` 风格分裂；同前缀能力（EXAM/EXAM_OPEN、RX_AUDIT/RX_ASSIST）风格不同。文档明文要求"统一契约"，但错误码命名分裂使契约一致性打折扣。
- **所在位置**：3.4.1（line 774）、3.4.2（line 788）、3.4.3（line 803）、3.4.4（line 827）、3.4.5（line 842）、3.4.6（line 856）、3.4.7（line 874）、3.4.8（line 887）、3.4.9（line 900）、3.4.10（line 914）、3.4.11（line 932）、3.4.12（line 958）、3.4.13（line 982）
- **严重程度**：一般
- **改进建议**：在 6.4 节或 3.4 节引言中明文约定统一错误码命名规则（如统一采用 `<能力前缀>_AI_TIMEOUT / _UNAVAILABLE / _INPUT_INVALID / _NO_RECOMMENDATION / _CONFLICT / _INTERNAL_*`），对不符合规则的 5 项（3.4.2 / 3.4.3 / 3.4.9 / 3.4.10 / 3.4.11）逐项修订，并将 3.4.1 的 `TRIAGE_INPUT_INVALID` 修订为 `TRIAGE_AI_INPUT_INVALID`。

- **问题描述**：3.1.3.2"AI 智能病情咨询"（患者端聊天式 AI 问询，line 286–298）业务场景完整，但未在全文内明示调用的是哪个 3.4.x AI 能力；3.4.8"AI 知识库问答"也仅泛指"AI 智能问诊"。两条业务线之间的归属关系在前 5 轮审议中从未明确，下游实施方按文档字面理解 3.1.3.2 既可能复用 3.4.8 也可能复用 3.4.1 或 3.4.4，存在契约歧义。
- **所在位置**：3.1.3.2"AI 智能病情咨询"（line 286–298）；3.4.8"AI 知识库问答"（line 880–891）
- **严重程度**：一般
- **改进建议**：在 3.1.3.2 关键交互段补"本节调用 3.4.8 AI 知识库问答能力，输入为患者自然语言问题、输出为 AI 文本回复"的显式契约引用；在 3.4.8 能力定义段同步增补"业务支撑关系：3.1.3.2 AI 智能病情咨询、医生工作台辅助问答等"。

- **问题描述**：6.3"异常降级与容错"作为总括节仅列出 5 项 AI 能力的降级路径（智能问诊、智能诊断、处方审核、病历生成、检查/检验 AI 报告与判读），未覆盖其余 8 项 AI 能力（3.4.7 AI 影像分析、3.4.8 AI 知识库问答、3.4.9 AI 开立检查/检验、3.4.10 AI 辅助开方、3.4.11 AI 执行顺序推荐、3.4.12 AI 医生排班、3.4.13 AI 综合讨论结论）；虽然各小节末尾有"降级路径"段，但 6.3 总览章未交叉索引，下游架构设计可能仅针对 5 项实现降级熔断而遗漏其余 8 项。
- **所在位置**：6.3"异常降级与容错"（line 1360–1368）
- **严重程度**：一般
- **改进建议**：将 6.3 重写为覆盖全部 13 项 AI 能力的降级路径总表（或注明"完整降级路径见 3.4.x 各小节降级路径段"），至少补齐 3.4.7/3.4.8/3.4.9/3.4.10/3.4.11/3.4.12/3.4.13 这 7 项在前 5 项之外的降级路径（如 AI 影像分析 → 医生手动判读、AI 知识库问答 → 暂不可用提示、AI 开立检查/检验 → 手动新增、AI 辅助开方 → 纯手动开方、AI 执行顺序推荐 → FIFO + 紧急度手动排序、AI 医生排班 → 纯人工排班、AI 综合讨论结论 → 医生手动汇总）。