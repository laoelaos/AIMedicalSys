根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

第 4 轮组件 B 诊断报告共识别 17 项质量问题，质询结论为 LOCATED（可信），全部需要在本轮（v5）迭代中落实。

### 严重（2 项，优先处理）

1. **3.3.2 AI 医生排班 缺 AI 服务契约，与 3.4.x 风格不一致**
   - 所在位置：3.3.2 AI 医生排班；3.4.x 全文（缺对应小节）
   - 改进建议：在 3.4.x 新增 3.4.12 AI 医生排班。输入契约至少包含 `constraints`（排班约束 object，含 `start_date` / `end_date` / `department_id` 列表 / `shift_rules` / `doctor_quota` 等子字段）、`doctor_pool`（候选医生信息）、`historical_schedule_ref`（历史排班引用，可选）；输出契约包含 `proposed_schedules`（推荐排班列表，每项含 `doctor_id` / `date` / `shift` / `workload_score`）与 `summary`（整体说明）；错误码 `SCHED_AI_TIMEOUT` / `SCHED_AI_UNAVAILABLE` / `SCHED_AI_CONFLICT`；硬超时与降级路径按 3.4.x 既有约定给出。

2. **3.4.5 与 3.4.7 之间的服务调用/组合关系未声明，导致 image_recognition 数据来源不明**
   - 所在位置：3.4.5 输入契约、3.4.5 输出契约（image_recognition 段）、3.4.7 输入契约、4.3.2 步骤 4–5
   - 改进建议：二选一——方案 A，明确 3.4.5 输出中的 `image_recognition` 字段为"可选子对象，仅在原始数据为影像类（CT/MRI/X-RAY）且 `model_id` 已被识别时返回"，其值由后端在 3.4.5 内部先调用 3.4.7 获取识别结果再组装；方案 B，将 `image_recognition` 从 3.4.5 中移除，由 3.4.5 调用方在业务流程中显式调用 3.4.7 并将结果与 3.4.5 的 `report_draft` 合并。无论选哪条，都需在 3.4.5 服务质量要求中加注"岗位权限：`image_recognition` 仅在岗位可用的影像模型范围内返回"。

### 一般（10 项）

3. **5.1 病历 实体 字段列表 缺 symptom_description，与 3.4.3 输出契约不匹配**
   - 所在位置：5.1 核心业务实体（病历）；3.4.3 输出契约（非流式字段列表）；3.2.2.2 输入约束
   - 改进建议：在 5.1 病历 实体字段中追加"症状描述（对应 3.4.3 输出 `symptom_description`，与 3.2.2.2 输入字段对称）"；同步在 4.3.1 门诊接诊全流程步骤 2 提示医生"5 个长文本字段均需录入"。

4. **5.1 缴费记录 实体"类型（收费/退费）"与"不直接生成新记录"逻辑自相矛盾**
   - 所在位置：5.1 核心业务实体（缴费记录）；3.1.7 创建入口（v3 修订段）；5.4.6 状态机（v3 命名统一段）
   - 改进建议：将 5.1 缴费记录 实体的"类型"字段修订为"业务类型（枚举：挂号费 / 检查费 / 检验费 / 处方费 / 其他）"（描述关联单据的费用类型，与 5.1 现有"关联单据标识"字段配合使用）；删除原"类型（收费/退费）"中的"退费"枚举值；在 5.4.6 状态机结尾加注"缴费记录实体的业务类型一旦创建后不可变，'退费'通过状态字段表达而非业务类型"。

5. **3.2.5 药房管理 与 3.2.6 药库管理 之间的库存关系未定义**
   - 所在位置：3.2.5 药房管理、3.2.6 药库管理、5.1 核心业务实体
   - 改进建议：方案 A——明确 3.2.6 药库 = 总库存，3.2.5 药房 = 药库日配额下的子库存（药房医生每日从药库调拨至药房），发药扣减药房库存、退药回补药房库存、药房定期盘库后差额回退药库；在 5.1 新增"药房库存""药库库存""库存调拨记录"三个实体；方案 B——简化设计为单层库存（仅药库），药房发药直接扣减药库库存并在发药记录中标记"由药房出库"，删除药房独立库存概念。

6. **3.4.3 AI 病历生成 流式输出 缺总时长硬超时**
   - 所在位置：3.4.3 服务质量要求（超时阈值段）；3.4.3 错误码（`MR_GEN_TIMEOUT`）
   - 改进建议：将 3.4.3 服务质量要求修订为"非流式 P95 ≤ 8 秒、硬超时 ≤ 12 秒；流式首字响应 ≤ 2 秒、整流式过程总时长硬超时 ≤ 30 秒、单分片间隔 ≤ 5 秒；任一阈值触发即返回 `MR_GEN_TIMEOUT` 并保留已生成的部分内容"。

7. **3.2.2.7 AI 处方审核 降级标签"规则校验通过"具有误导性**
   - 所在位置：3.2.2.7 异常分支（AI 调用超时阈值段）、3.4.2 服务质量要求（降级路径段）
   - 改进建议：将"规则校验通过"占位标签修订为"AI 不可用，已回退本地规则校验（未通过 AI 审核）"或类似措辞；在医生端 UI 中显著位置标识本次未经过 AI 审核，避免医生误判；7.1 验收项 #12（AI 服务不可用演示降级路径）同步明示该标签。

8. **3.4.4 与 3.4.10 patient_info 字段集不一致，违反 6.4 "统一的契约"要求**
   - 所在位置：3.4.4 输入契约、3.4.10 输入契约、6.4 扩展性与可替换性（"统一的、向后兼容的契约"）
   - 改进建议：将 `patient_info` 字段集统一为 `{ patient_id, age, gender, allergy_history, comorbidities }`，3.4.4 中 `patient_id` 与 `comorbidities` 改为必填或可选需在字段级标注（建议 `comorbidities` 必填）；同步检视 3.4.9、3.4.2 等其他 AI 能力的 `patient_info` 字段集是否一致。

9. **7.1 #5 验收项将 5 个独立 AI 能力合并为一项，验收粒度过粗**
   - 所在位置：7.1 功能验收 #5
   - 改进建议：将 7.1 #5 拆分为 5 个独立验收项（#5a 智能诊断、#5b AI 开立检查/检验、#5c AI 辅助开方、#5d AI 处方审核、#5e AI 病历生成），每项均要求"医生在医生端完成对应能力的端到端调用并得到可观察的 AI 返回结果"；原 #5 保留作为汇总要求（"以上 5 项 AI 能力均需在演示中覆盖"）。

10. **3.1.3.1 AI 智能导诊 追问回答 与 chief_complaint 的组装机制未明确**
    - 所在位置：3.1.3.1 关键交互与输入、3.4.1 输入契约
    - 改进建议：在 3.1.3.1 关键交互或 3.4.1 输入契约中追加"组装约定"段，明确：①若为单次调用，前端按"主诉 + 追问 Q&A 列表"格式拼接为单一 `chief_complaint` 字符串，字符数上限 5–500 适用拼接后总长；②若为多轮调用，首次调用仅传主诉，后续追问通过同一 `session_id` 增量传入 `additional_responses` 数组；③任一方式下，3.4.1 服务侧需将完整信息组装为推理 prompt。建议在 3.4.1 中显式定义 `additional_responses` 数组字段（可选），同时保留单字段 `chief_complaint` 兼容简单场景。

11. **3.2.5 退药校验"是否被患者实际取走"的取走状态未在 5.1 实体中定义**
    - 所在位置：3.2.5 药房管理（异常分支）、5.1 核心业务实体（发药/退药记录）、5.4.2 处方状态机
    - 改进建议：方案 A——将 5.4.2 处方状态机"已发药"细分为"已发药（待取）"与"已发药（已取）"，新增 `dispensed_at`（发药时间）与 `picked_up_at`（取药时间）字段；3.2.5 退药校验改为"取药时间在 N 小时内允许退药"（如 24 小时内），超时不允许退药或走特殊流程。方案 B——简化设计：处方"已发药"即认为"已实际交付"，退药仅处理"已发药但未取走"的情况，由药房医生在发药时同步完成取药登记。

12. **3.3.3 AI 分诊台 维护的规则 与 3.4.1 智能分诊 推理 之间的消费机制未声明**
    - 所在位置：3.3.3 AI 分诊台（关键交互与输出）、3.4.1 输入契约与输出契约
    - 改进建议：在 3.4.1 输入契约中追加可选字段 `rule_version`（string，配置规则版本号）和 `rule_set_id`（string，配置规则集标识）；输出契约追加 `matched_rules`（array，含 `rule_id` / `confidence` / `reason`），用于说明本次推理命中的规则；在 3.3.3 关键交互中说明"规则版本变更后需触发 3.4.1 推理策略的热更新或冷切换"。

### 轻微（5 项）

13. **3.3.1 智能排班 排班冲突描述 与 5.1 医生"所属科室"为单数存在隐含矛盾**
    - 所在位置：3.3.1 智能排班 异常分支、5.1 核心业务实体（医生）
    - 改进建议：将 3.3.1 排班冲突描述改为"排班冲突（同一医生同时段被多条排班记录占用）时需提示"，去除"被多科室占用"措辞；同步明确 5.1 医生 实体的"所属科室"为单科室（与 2.3.1 门诊医生"所属科室"一致），但允许医生在不同时段跨科室出诊（在排班记录中体现）。

14. **3.4.11 AI 执行顺序推荐 输入 `urgency_hint` 与 输出 `priority` 映射关系未定义**
    - 所在位置：3.4.11 输入契约（task_items 子字段）、3.4.11 输出契约（execution_order 子字段）
    - 改进建议：在 3.4.11 中追加"输入输出映射约定"段，明确"`urgency_hint=HIGH` 倾向于 `priority=P1`、但最终 `priority` 由 AI 综合 `task_items` 全部字段与 `context` 后给出"；或在服务质量要求中加注"`priority` 与 `urgency_hint` 允许不一致，最终以 `priority` 为准"。

15. **3.1.6 健康档案 字段约束仅覆盖过敏史/慢病，家族史/手术史/用药史等字段无约束**
    - 所在位置：3.1.6 输入约束、5.1 核心业务实体（健康档案）
    - 改进建议：将 3.1.6 输入约束扩展为覆盖全部健康档案列表字段："过敏史/慢病/家族史/手术史/用药史等列表字段每项字符数 ≤ 200；新增条目需校验名称非空且不重复；每项可附带发生时间或关系（家族史用）等扩展属性"。

16. **2.3.5 线下接诊医生 角色定位"医生或工作人员"措辞模糊**
    - 所在位置：2.3.5 业务定位、2.1 角色总览
    - 改进建议：将 2.3.5 业务定位改为"在线下窗口为患者提供挂号、收费等服务的医生岗位"；如确实允许非医生背景人员承担，明确为新角色"线下窗口工作人员"并单独成节，与"线下接诊医生"区分；2.6 权限矩阵同步刷新。

17. **3.2.10 撤销权限 与 2.6 矩阵"医嘱开立与记账"行未严格对齐**
    - 所在位置：3.2.10 关键交互、2.6 权限矩阵"医嘱开立与记账"行
    - 改进建议：将 3.2.10 修订为"已记账但未支付的待缴费单据可由开单医生本人撤销；开单医生离职或不可用时，由管理员代为撤销并留痕"；或在 2.6 矩阵"医嘱开立与记账"行末尾加注"包含开单医生本人撤销与管理员代撤销两个子动作"。

## 历史迭代回顾

历史共 4 轮迭代反馈，本轮为第 5 轮。

### 已解决的问题

以下问题在前 4 轮反馈中曾被指出，但在本轮（第 5 轮）的 17 项反馈中均未再提及，可视为已闭环：

- **第 1 轮**：#1 非功能需求量化（已通过 6.7/3.4.x 各项阈值落实）、#2 AI 服务契约（已通过 3.4.1–3.4.12 落地）、#3 线下接诊管理缺失（已通过 3.2.9 补齐）、#4 业务规则量化（已通过 v2/v3 各阈值落实）、#5 健康档案实体缺失（已补齐）、#6 缴费记录来源（已通过 3.1.7 明确）、#7 功能-角色可访问矩阵（已补齐 2.6）、#8 取消规则自引用（已消解）、#9 住院/便民服务范围（已通过 3.1.8 入口可达性矩阵明确）、#11 占位描述（已细化）、#12 AI 分诊台边界（已在 v2/v3/v4 分层为 3.3.3/3.4.1/3.4.11）、#13 关键实体状态机（已建立 5.4.x 系列）。
- **第 2 轮**：#1 处方记录查询（已在 2.6 与 3.1.5 落实）、#2 医嘱开立与记账边界（已通过 v3 重构 3.2.10）、#3 处方 1-N 审核记录（已在 5.3 改为 1-N 并新增"审核次序"字段）、#4 AI 开立检查/检验 + AI 辅助开方契约（已在 3.4.9/3.4.10 落实）、#5 药库管理员幽灵角色（已删除）、#6 医生端系统管理边界（已收窄至 3.2.7 个人资料维护）、#7 status 字段（已在 3.4.6/3.4.5 落实）、#8 已取消 vs 已退号（已在 v3 通过 `cancellation_channel` 区分）、#9 处方状态机中间态（已在 5.4.2 拆分"已提交"与"待发药"）、#10 智能诊断 condition 结构化（已在 3.4.4 落实）、#15 退费动作边界（已通过 v3"不直接生成新记录"机制明确）、#16 ○ 含义细化（已通过 ○¹/○² 区分）、#17 纯需求文档张力（v4 已降低相关技术流程措辞）。
- **第 3 轮**：#1 4.2.3 取消挂号流程（已修订）、#2 7.1 #8 退药联动（已修订）、#3 5.4.1 关联影响段（已修订）、#4 诊断超时阈值矛盾（已统一为 12 秒）、#5 审核超时阈值矛盾（已统一为 6 秒）、#6 5.4.2 审核通过触发条件（已补充）、#8 3.4.11 AI 执行顺序推荐（已独立小节化）、#10 3.4.5 delta 字段（已在 v4 移除/改名为 comparison_summary）。
- **第 4 轮后续**（本轮与第 4 轮部分 v4 修订说明条目中已闭环）：v2 #13 symptom_description（3.4.3 已加，但 5.1 实体未同步——见下方持续问题）、v2 #14 医生最终决定持久化（已在 5.1 处方审核记录实体落实）。

### 持续存在的问题（需重点解决）

- **AI 业务能力的字段级契约完备性**：
  - 第 1 轮 #2（AI 契约整体缺失）→ 第 2 轮 #4（AI 开立检查/检验 + AI 辅助开方契约缺失）→ 第 4 轮 #1（AI 排班契约缺失）
  - 表现：每一轮都会发现"遗漏了某个 AI 业务能力的 3.4.x 契约"。本轮第 5 轮需彻底补齐 3.4.12 AI 排班，并审视是否还有未发现的 AI 业务能力（建议对照 1.4 节六大领域与 3.x 全章节列举的 AI 调用点）。
- **5.1 实体字段 与 AI 输出契约的对齐**：
  - 第 2 轮 #13（3.4.3 输出缺 symptom_description）→ 第 4 轮 #3（5.1 病历 实体仍未列出 symptom_description 字段）
  - 表现：修订只更新了 AI 契约侧，未同步更新持久化层实体定义。本轮需补齐病历实体字段，并同步排查 5.1 其他实体（如缴费记录 类型字段——见第 4 轮 #4）是否也存在契约-实体不同步问题。
- **AI 服务间调用关系的隐含假设**：
  - 第 4 轮 #2（3.4.5 ↔ 3.4.7 调用关系未声明）
  - 表现：多个 AI 服务的运行时组合关系在文档中以"隐含顺序"存在。本轮需补齐 3.4.5 ↔ 3.4.7 的调用约定，并建议对 3.4.x 全章节做一次"是否有调用其他 AI 服务"的扫描（例如 3.4.5 是否还会调用 3.4.6、3.4.8 是否被 3.4.3 调用等）。
- **状态机完整性**：
  - 第 2 轮 #9（处方状态机缺"待发药"中间态）→ 第 4 轮 #11（处方状态机缺"待取/已取"细分）
  - 表现：状态机的中间态遗漏反复出现。本轮需补齐发药后的"待取/已取"细分，并建议对 5.4.x 全系列做一次"是否存在未观测中间态"的体检。

### 新发现的问题（本轮第 5 轮新识别）

- 第 4 轮 #2 3.4.5 ↔ 3.4.7 调用关系
- 第 4 轮 #4 5.1 缴费记录 类型字段与"不直接生成新记录"机制矛盾
- 第 4 轮 #5 药房/药库库存关系未定义
- 第 4 轮 #6 3.4.3 流式输出缺总时长硬超时
- 第 4 轮 #7 AI 处方审核降级标签"规则校验通过"误导性
- 第 4 轮 #8 3.4.4 与 3.4.10 patient_info 字段集不一致
- 第 4 轮 #9 7.1 #5 验收粒度过粗
- 第 4 轮 #10 3.1.3.1 追问组装机制未明确
- 第 4 轮 #12 3.3.3 与 3.4.1 规则消费机制未声明
- 第 4 轮 #13 医生单科室与多科室排班冲突描述隐含矛盾
- 第 4 轮 #14 3.4.11 urgency_hint 与 priority 映射未定义
- 第 4 轮 #15 健康档案字段约束不全
- 第 4 轮 #16 2.3.5 线下接诊医生角色定位模糊
- 第 4 轮 #17 3.2.10 撤销权限边界模糊

## 上一轮产出路径

`C:\Develop\Software\AIMedicalSys\Harness\redeliberations\202606141830_smart_clinic_requirements\a_v4_req_v2.md`

## 用户需求

`C:\Develop\Software\AIMedicalSys\Harness\redeliberations\202606141830_smart_clinic_requirements\requirement.md`