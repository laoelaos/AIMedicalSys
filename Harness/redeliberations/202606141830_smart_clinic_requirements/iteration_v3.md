# 再审议判定报告（v3）

## 判定结果

RETRY

## 判定理由

组件B诊断报告识别出 v3 产出存在 **6 项严重问题**与 **5 项一般问题**。组件B质询报告结论为 **LOCATED**（已被确认），实际轮次 1 < 最大轮次 12，质询在第 1 轮即完成，且质询仅补充了一处轻微的"同一根因下第三处实例"覆盖缺口（5.4.1 line 1106–1108 关联影响段），未否定任何一项已识别问题，全部 6 项严重问题与 5 项一般问题均通过证据充分性、逻辑完整性审查。

按判定标准：审查报告包含严重或一般等级的问题 → **RETRY**。

## 需要解决的问题

- **问题描述**：4.2.3 取消挂号流程步骤 4 仍按 v1 时代表述（"可能的"退费缴费记录），与 v3 修订的"不直接生成新记录"机制冲突
- **所在位置**：4.2.3 步骤 4（line 941）
- **严重程度**：严重
- **改进建议**：将步骤 4 改为"系统生成'取消'状态变更；若该挂号已支付且无发药联动，则将原缴费记录状态变更为'待退费'（不创建新记录），由线下窗口按 3.2.9 实际完成退费"；移除"可能的"模糊措辞；与 3.1.4.1 / 3.1.7 / 5.4.6 保持一致

- **问题描述**：7.1 验收 #8 "退药成功时联动生成'退费'缴费记录" 与 v3 修订的"不直接生成新记录"规则直接冲突
- **所在位置**：7.1 #8（line 1290）
- **严重程度**：严重
- **改进建议**：将 7.1 #8 改为"药房医生完成发药与退药；退药成功时联动将关联的'已支付'缴费记录状态变更为'待退费'（不生成新记录），由线下窗口按 3.2.9 完成实际退费"

- **问题描述**：5.4.1 挂号记录状态机"关联影响"段（PATIENT / OFFLINE_WINDOW 渠道）仍写"联动生成'退费'缴费记录"，与 5.4.6 / 3.1.7 不冲突，但需同步刷新（质询补充发现的第三处实例）
- **所在位置**：5.4.1 line 1106–1108
- **严重程度**：严重
- **改进建议**：将 5.4.1 关联影响段中"联动生成'退费'缴费记录"改为"联动将关联'已支付'缴费记录状态变更为'待退费'"，与 5.4.6 / 3.1.7 对齐；建议作者全文检索"生成.{0,4}退费.{0,4}缴费记录"等关键短语，扫除该机制下其余可能漂移点

- **问题描述**：AI 智能诊断超时阈值在 3.2.2.3（30 秒）与 3.4.4（硬超时 ≤ 12 秒）存在量级矛盾
- **所在位置**：3.2.2.3 line 456、3.4.4 line 786
- **严重程度**：严重
- **改进建议**：以 3.4.4 硬超时 ≤ 12 秒为准，将 3.2.2.3 修订为"硬超时 12 秒"；或反向以 3.2.2.3 为准并同步刷新 3.4.4 与 6.7。任一选项都必须保证全文档同口径

- **问题描述**：AI 处方审核超时阈值在 3.2.2.7（15 秒）与 3.4.2（硬超时 ≤ 6 秒）存在量级矛盾
- **所在位置**：3.2.2.7 line 519、3.4.2 line 750
- **严重程度**：严重
- **改进建议**：以 3.4.2 硬超时 ≤ 6 秒为准，将 3.2.2.7 改为"AI 调用超时阈值：硬超时 6 秒（与 3.4.2 一致）"

- **问题描述**：5.4.2 处方状态机"审核通过"的触发条件未定义，与 3.2.2.7 风险等级规则无法对齐
- **所在位置**：5.4.2 状态机（line 1116–1128）
- **严重程度**：严重
- **改进建议**：在 5.4.2 触发条件中补充：待审核 → 已提交（LOW 风险 / MEDIUM 风险下医生确认强制提交留痕）；待审核 → 草稿（HIGH 风险阻断 / 医生主动撤销审核）

- **问题描述**：7.4 交付物验收遗漏管理端，与 7.1 #10 验收要求不对应
- **所在位置**：7.4 line 1312
- **严重程度**：严重
- **改进建议**：7.4 前端项改为"前端：患者端、医生端、管理员端主要页面的可运行工程源码、关键页面截图或录屏"

- **问题描述**：3.2.3 / 3.2.4 中"AI 分诊台"复用 3.4.1 智能分诊，但 3.4.1 输入契约（chief_complaint）与新用途（检查/检验项目执行顺序）在输入/输出语义上不匹配
- **所在位置**：3.2.3 line 555、3.2.4 line 574、3.4.1 line 729–739、4.3.2 / 4.3.3 line 962–973
- **严重程度**：一般
- **改进建议**：方案 A——在 3.4.1 输入契约中追加"扩展输入模式"：当 mode=EXECUTION_PRIORITY 时，输入为 task_items 而非 chief_complaint，输出为 execution_order 与 priority；方案 B——在 3.4.x 新增独立"3.4.11 AI 执行顺序推荐"能力小节

- **问题描述**：6.7 "关键 AI 调用最长 30 秒用户等待提示" 与 3.4.x 硬超时实际值不匹配（12s / 6s / 8s 均远小于 30s），构成运行时不可达规范
- **所在位置**：6.7 line 1259
- **严重程度**：一般
- **改进建议**：将"30 秒"修订为各 AI 能力硬超时 + 合理缓冲（如"硬超时 + 5s 网络缓冲"），或将等待提示时长与具体 AI 能力在 3.4.x 中明确对应；删除"30 秒"未触发的不可达规范

- **问题描述**：3.4.5 AI 智能检查报告输出契约 delta 字段（与上一次同项目结果的差值 float）不适用于影像类检查（CT/MRI/X-RAY）
- **所在位置**：3.4.5 line 796、line 802
- **严重程度**：一般
- **改进建议**：将 delta 字段从 3.4.5 中移除（仅在 3.4.6 数值型检验报告中保留），或在 3.4.5 中改名为 comparison_summary（结构化对比摘要），明确允许为空

- **问题描述**：3.4.9 输入契约 condition_text（单 string）与 3.4.4 结构化 condition 对象风格不一致，与 6.4 "统一的"契约要求冲突
- **所在位置**：3.4.4 line 773、3.4.9 line 852、6.4 line 1237
- **严重程度**：一般
- **改进建议**：将 3.4.9 的 condition_text 改为结构化对象 condition（与 3.4.4 同结构），并在组装约定中明确 5 个子字段如何拼接；或为该不一致提供明确的合理化说明

- **问题描述**：6.7 性能指标中多项"（推断）"标注未在 7.1 / 7.3 验收联动明示
- **所在位置**：6.7 line 1267、7.1 全文、7.3 全文
- **严重程度**：一般
- **改进建议**：在 7.1 / 7.3 中追加一条"6.7 中标注'（推断）'的指标由业务方与教学方在评审会议中确认后另行纳入正式验收，本轮演示阶段不构成拒收依据"
