# 再审议判定报告（v11）

## 判定结果

RETRY

## 判定理由

组件B诊断报告共识别 9 项问题，其中包含 5 项"一般"严重等级问题（#2 处方状态机缺少终态完整性约束、#3 健康档案列表字段结构深度不足、#5 流式输出超时错误响应契约未完全闭合、#7 image_recognition 的 model_id 选择机制未明确、#9 本地规则校验的具体范围未定义）。根据判定标准，审查报告包含一般等级问题，满足 RETRY 条件。质询报告为空，质询未完成，无需考虑质询结论。

## 需要解决的问题

- **问题描述**：5.4.2 处方状态机缺少终态完整性约束，"已退药"未声明是否为终态
- **所在位置**：§ 5.4.2 "不允许的流转"（line 1365）
- **严重程度**：一般
- **改进建议**：在 5.4.2 "不允许的流转"段末尾追加"已退药 → 任何其他状态（终态）"

- **问题描述**：3.1.6 健康档案列表字段（过敏史、用药史、手术史等）仅以"字符数 ≤ 200"描述，缺少结构化子字段定义
- **所在位置**：§ 3.1.6 输入约束（line 381）；§ 5.1 健康档案实体（line 1235）
- **严重程度**：一般
- **改进建议**：扩展为明确的结构化定义，或明确标注由架构设计阶段定义

- **问题描述**：3.4.3 流式超时场景下错误响应契约未闭合，"返回"语义在流式通道中不明确
- **所在位置**：§ 3.4.3 服务质量要求（line 837-844）
- **严重程度**：一般
- **改进建议**：追加"超时错误分片"格式说明，或明示流式超时降级到非流式回退

- **问题描述**：3.4.5 的 model_id 选择机制未明确，输入中无 model_id 字段，后端自动推断规则未定义
- **所在位置**：§ 3.4.5 "与 3.4.7 的服务组合关系"（line 870）；输出契约（line 874）
- **严重程度**：一般
- **改进建议**：补充 model_id 选择规则及权限校验先后顺序

- **问题描述**："本地规则校验"的定义开放，不同理解下实现复杂度差异巨大
- **所在位置**：§ 3.4.2 服务质量要求（line 824）；§ 3.2.2.7 异常分支（line 567）
- **严重程度**：一般
- **改进建议**：明示"本地规则校验"的检查项边界，或标注由架构设计阶段定义
