# 再审议判定报告（v13）

## 判定结果

RETRY

## 判定理由

组件B诊断报告确认5项存留问题，质询报告结果为LOCATED（实际轮次1 < 最大轮次12，提前终止）。问题1严重程度为"中"，对应「一般」等级，满足RETRY触发条件（审查报告包含一般等级问题）。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：§3.4.5内部调用§3.4.7时`image_ref`的数据来源未定义，`raw_data_ref`与`image_ref`的映射关系及非影像类检查的传递规则存在二义性
- **所在位置**：§3.4.5"与3.4.7的服务组合关系"段；§3.4.7输入契约`image_ref`字段
- **严重程度**：一般
- **改进建议**：在§3.4.5中明确`raw_data_ref`到`image_ref`的映射规则，非影像类检查不传递此值

---

- **问题描述**：§5.4.3"采纳到病历"分支的前提状态未明确定义，"草稿"状态下点击采纳的路径未被状态机覆盖
- **所在位置**：§3.2.8关键交互"采纳到病历"段、§5.4.3病历状态机"AI综合讨论结论采纳"分支
- **严重程度**：一般
- **改进建议**：在§5.4.3中明确采纳分支可触发的前提状态（"草稿"和"草稿（修订后）"），同步更新§3.2.8的交互约束

---

- **问题描述**：§3.4.13 `discussion_text < 50`字符时无对应的阻断/重试/告警机制
- **所在位置**：§3.4.13输出契约`discussion_text`字段描述、服务质量要求"短文本降级提示"段
- **严重程度**：轻微
- **改进建议**：追加50字符门槛的对应处理规则（返回错误码或前端强提示）

---

- **问题描述**：§3.4.6 `delta`字段中"上一次"的排序规则未指定，多版本场景下计算不一致
- **所在位置**：§3.4.6输出契约`abnormal_items`子字段`delta`描述
- **严重程度**：轻微
- **改进建议**：明确排序依据（按报告提交时间降序，多版本取最近提交版本）

---

- **问题描述**：§3.1.6健康档案→AI契约过敏信息映射仅传递过敏原名称，不传递反应类型与严重程度，存在临床安全风险
- **所在位置**：§3.1.6末段"健康档案数据→AI契约字段的映射约定"
- **严重程度**：一般
- **改进建议**：在§3.1.6追加业务方评审提示，建议确认是否需将`severity`和`reaction_type`纳入AI输入契约
