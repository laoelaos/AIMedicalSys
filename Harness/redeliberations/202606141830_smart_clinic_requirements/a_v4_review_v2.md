# 需求文档审查报告（v2）

## 审查结果

APPROVED

## 逐维度审查

### 1. 忠实性

**[通过]** v4 文档严格围绕本轮迭代需求（a_v4_iteration_requirement.md）中列出的 6 项严重问题 + 1 项质询补充 + 5 项一般问题 + 6 项轻微问题（共 18 项）逐项落实，每项修复均在文档相应章节落地，且在第 12 节"修订说明（v4）"中提供一一对应的处理记录。

**[通过]** 7 项严重问题的修复全部到位：
- 4.2.3 步骤 4（line 960）：已改为"系统生成'取消'状态变更；若该挂号已支付且无发药联动，则将原缴费记录状态变更为'待退费'（不创建新记录），由线下窗口按 3.2.9 实际完成退费；若该挂号已发药则先联动走 3.2.5 退药流程"——移除"可能的"模糊措辞。
- 7.1 #8（line 1314）：已改为"退药成功时联动将关联的'已支付'缴费记录状态变更为'待退费'（不生成新记录），由线下窗口按 3.2.9 完成实际退费"。
- 5.4.1 PATIENT / OFFLINE_WINDOW 渠道关联影响（line 1125-1127）：已统一为"联动将关联的'已支付'缴费记录状态变更为'待退费'（不创建新记录），由线下窗口按 3.2.9 完成实际退费"。
- 3.2.2.3（line 458）：超时阈值已改为"硬超时 12 秒（与 3.4.4 契约侧硬超时 ≤ 12 秒一致）"。
- 3.2.2.7（line 521）：AI 调用超时阈值已改为"硬超时 6 秒（与 3.4.2 契约侧硬超时 ≤ 6 秒一致）"。
- 5.4.2 触发条件（line 1141-1148）：已显式补充"待审核 → 已提交（未到药房）：AI 返回 LOW 风险（审核通过），或 MEDIUM 风险下医生确认强制提交（留痕）"与"待审核 → 草稿：AI 返回 HIGH 风险（系统阻断并要求医生修改），或医生主动撤销审核请求"，并新增"与 3.2.2.7 风险等级对齐（v4 补齐）"小节明确 LOW/MEDIUM/HIGH 三档语义。
- 7.4（line 1338）：前端项已改为"前端：患者端、医生端、管理员端主要页面的可运行工程源码、关键页面截图或录屏"，含管理员端。

**[通过]** 5 项一般问题全部修复：
- 3.4.11 新增"AI 执行顺序推荐"独立能力（line 879-894），输入 task_items、输出 execution_order/priority，3.2.3 / 3.2.4 / 4.3.2 / 4.3.3 全部切换为调用 3.4.11 而非 3.4.1，"AI 分诊台"概念边界明确。
- 6.7 用户等待提示（line 1282）：已修订为"关键 AI 调用（智能诊断 12s、处方审核 6s、病历生成 8s 等，对应 3.4.x）建议在前端做'硬超时 + 5s 网络缓冲'的用户等待提示（推断）"，并明示与 3.4.x 一一对应。
- 3.4.5 输出契约：已将原 delta 字段替换为 comparison_summary（结构化对比摘要，可空），3.4.6 数值型检验报告保留 delta 字段。
- 3.4.9 输入契约：已将 condition_text 改为结构化对象 condition，与 3.4.4 同结构（line 854）。
- 7.1 #13（line 1319）与 7.3（line 1333）：已分别追加"6.7 中标注'（推断）'的指标由业务方与教学方在评审会议中确认后另行纳入正式验收；本轮演示阶段不构成拒收依据"的联动说明。

**[通过]** 6 项轻微问题全部修复：
- 5.4.6 命名一致性（line 1219）：已新增"命名一致性（v4 修订）"小节，明确"已撤销"指缴费记录实体自身的撤销终态，与 5.4.1 挂号记录"已撤销"同字但无联动关系；实际退费路径统一为"已支付 → 待退费 → 已退费"。
- 5.1 缴费记录实体（line 1065）：已追加"原始金额""已退金额""未退金额"三个字段，对应 5.4.6 "已支付 → 待退费（部分）"分支。
- 6.7（line 1283）：已新增"AI 开立检查/检验推荐阈值（v4 补齐）：AI 推荐项目数告警阈值 ≥ 10（推断）"。
- 2.6 矩阵下方（line 193）：已追加"命名一致性说明（v4 修订）"段，明确"缴费记录"与"费用记录"指向同一业务实体，仅展示字段与权限范围不同。
- 3.2.2.1（line 429）：健康档案"只读"约束已扩展为"门诊医生、检查医生、检验医生、药房医生端均为'只读'（推断），修改入口仅在患者端 3.1.6 开放"，覆盖全部医生岗位。
- 5.4.2 状态机流转图"留痕后提交"/"留痕后入队"语义问题：已删除两个原标签，统一为"已提交（留痕）"与"待发药（留痕）"，并与 3.2.2.7 风险等级（LOW/MEDIUM/HIGH）显式对齐。

**[通过]** v3 严重问题 #3 质询补充第三处实例（5.4.1 挂号记录状态机关联影响段 PATIENT / OFFLINE_WINDOW 渠道）已修订：line 1126-1127 已统一为"联动将关联的'已支付'缴费记录状态变更为'待退费'（不创建新记录），由线下窗口按 3.2.9 完成实际退费"，OFFLINE_WINDOW 渠道描述与 PATIENT 渠道保持一致。

**[通过]** 全文检索"生成.{0,6}退费.{0,6}缴费记录""联动生成.{0,4}退费"关键短语，4 处命中均位于第 12 节"修订说明（v4）"中作为修订记录引用，非业务正文内容；正文中所有退费机制描述均统一使用"将原'已支付'缴费记录状态变更为'待退费'（不创建新记录）"措辞。"可能的"措辞仅在 3.1.3.2 AI 智能病情咨询"可能的追问建议"语境中使用，语义合理，与 v3 审查要求的"移除'可能的'模糊措辞"无关。

**[通过]** 9 章需求追溯矩阵已扩展至 REQ-082，其中 v4 新增条目 REQ-065 至 REQ-082 共 18 条，每条均与第 12 节"修订说明（v4）"18 项处理记录一一对应，溯源完整。

**[通过]** 文档未引入用户原始需求（requirement.md）之外的新业务领域；推断内容均以"（推断）"标注，与原始描述可清晰区分；无技术栈、框架、库选型等违反"纯需求文档"约束的内容。

### 2. 清晰性

**[通过]** 文档章节组织遵循"业务背景 → 角色与权限 → 功能需求 → 业务流程 → 数据需求 → 非功能需求 → 验收要求 → 范围与约束 → 需求追溯 → 修订说明"的清晰脉络，与 requirement.md 要求的结构完全一致。

**[通过]** 状态机描述（5.4 节）以 ASCII 流程图 + 触发条件 + 不允许流转三段式呈现，读者可同时看到宏观流转与微观规则；3.4.x AI 能力采用统一的"能力定义 / 输入契约 / 输出契约 / 服务质量要求"四段结构，3.4.11 与 3.4.1 的边界通过"能力边界"小节独立说明，下游架构设计者可据此正确区分患者主诉分诊与检查/检验执行顺序推荐两个不同 AI 能力。

**[通过]** 2.6 权限矩阵 + "○¹/○²"受限访问语义细分 + 命名一致性说明段，使读者可正确理解不同医生岗位对处方记录、健康档案等实体的访问范围；5.4.6 与 5.4.1 同字"已撤销"的边界通过"命名一致性"段显式说明，避免歧义。

**[通过]** 退费机制在 3.1.4.1（取消挂号）、3.1.7（缴费记录创建入口）、3.2.5（药房退药）、3.2.9（线下窗口退费）、3.2.10（医嘱开立与记账）、4.2.3（取消挂号流程）、4.3.4（药房发药/退药流程）、5.4.6（缴费记录状态机）八个位置均显式串联到"待退费"中间态，由线下窗口最终完成实际退费，下游可准确把握跨章节协同关系。

**[问题-轻微]** 6.7（line 1282）"病历生成 8s"在条目列举上与 3.4.3（line 767）的 P95 ≤ 8 秒（非硬超时）口径对齐——智能诊断 12s 与处方审核 6s 均为 3.4.x 的硬超时值，而 3.4.3 仅给出 P95 而无显式硬超时，导致 6.7 中"病历生成 8s"严格意义上属于 P95 而非硬超时。该不一致不影响功能语义（病历生成等待提示时长与契约层 P95 一致即可），但表述精度上有微小偏差；建议下轮在 3.4.3 补充显式硬超时值（如 12 秒）以使 6.7 三项列举口径完全统一。

### 3. 完备性

**[通过]** v3 严重问题修复覆盖度完整：退费机制文档同步（严重 #1、#2、#3）三项实例均已修复；AI 超时阈值量级矛盾（严重 #4、#5）两项已统一；处方状态机触发条件（严重 #6）已显式补齐；交付物验收（严重 #7）已补管理员端。

**[通过]** v3 一般问题修复覆盖度完整：3.4.11 新增独立能力、6.7 等待提示口径重写、3.4.5 delta 字段替换、3.4.9 输入契约结构化、7.1/7.3 验收与 6.7（推断）指标联动——五项一般问题均无遗漏。

**[通过]** v3 轻微问题修复覆盖度完整：5.4.6 命名统一、5.1 部分退费字段补齐、3.2.2.4 阈值迁入 6.7、2.6 命名一致性说明、3.2.2.1 只读约束扩展、5.4.2 留痕语义对齐——六项轻微问题均无遗漏。

**[通过]** v3 跨轮持续存在的问题（AI 分诊台概念边界、6.7（推断）指标与验收联动、退费机制文档同步、AI 输入契约结构化、AI 超时阈值多口径一致性）均在 v4 得到彻底解决，溯源至 REQ-072、REQ-076、REQ-065/066/067、REQ-075、REQ-068/069。

**[通过]** 文档总长 1524 行，未出现因修订遗留导致的关键信息缺失；所有 v3 残留的"加戏""遗漏"或"歧义"问题已通过 v4 修复闭环。

## 修改要求

无（APPROVED 不含一般及以上等级问题）。