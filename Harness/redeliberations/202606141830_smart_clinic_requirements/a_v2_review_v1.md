# 需求文档审查报告（v1）

## 审查结果

APPROVED

## 逐维度审查

### 1. 忠实性

**[通过]** 文档严格基于三份原始文档（01/02/03）组织内容，未发现"加戏"或偏离用户原意之处；所有功能点、角色、流程均能溯源到原始文档。

**[通过]** 本轮 M=2 迭代要求的 13 项问题（2 项高、7 项中、4 项低）已逐项落实，并在第 10 章"修订说明"中以可核对的表格形式与对应位置/动作映射，便于下游交叉验证。

**[通过]** 文档对推断部分（如"AI 智能讨论结论"输入字段、"AI 调用超时阈值（推断）"标注等）以"（推断）"明示，未冒充用户原意。

**[通过]** 第 8 章"范围与约束"对"住院服务""AI 智能讨论结论""线下接诊医生职责扩展"等边界进行了明确化，避免了 v1 范围模糊的问题。

### 2. 清晰性

**[通过]** 3.4.1–3.4.8 八个 AI 能力均以"输入契约 / 输出契约 / 服务质量要求"三段式表达，字段名、类型、必填性、取值范围、错误码与降级路径同段呈现，下游架构设计者无需跨章节拼凑。

**[通过]** 2.6"功能-角色可访问矩阵"覆盖 7 类角色 × 28 个功能点，对受限访问（○）给出统一含义说明（仅本人责任范围数据），可读性强。

**[通过]** 5.4 状态机章节采用 ASCII 状态图 + 触发条件 + 禁止流转三段式呈现，6 个核心实体（挂号/处方/病历/检查报告/检验报告/缴费记录）的状态流转规则清晰可读。

**[通过]** 3.1.4 与 3.1.4.1 之间不再有循环引用：3.1.4 异常分支改为"与 3.1.4.1 协同判断"提示，3.1.4.1 自包含完整规则集（时间窗、库存联动、缴费联动、审批约束）。

**[通过]** 3.3.3"AI 分诊台"与 3.4.1"AI 智能分诊"在第 3.3.3 顶部以"业务定位（v2 明确）"段落给出"配置入口 vs 推理能力"边界说明，消除了 v1 的概念混淆。

**[通过]** 6.7 性能与可靠性使用统一的量化指标结构（P95 时延 / 硬超时 / 召回率 / 并发 / 可用性），便于下游测试团队直接落地为验收用例。

### 3. 完备性

**[通过]** 高严重度 1（非功能指标量化）：6.7 给出挂号/病历/报告查询 P95 ≤ 1s、详情查询 P95 ≤ 500ms；各 AI 能力 P95 与硬超时（3.4.1 5/8s、3.4.2 3/6s、3.4.3 8s 非流式/2s 流式首字、3.4.4 8/12s、3.4.5 10/15s、3.4.6 6/10s、3.4.7 12/20s、3.4.8 4/8s）；分诊命中率 ≥ 60%、处方审核高风险召回率 ≥ 95%；并发 ≥ 50 用户 / ≥ 100 QPS；可用性 ≥ 99%。7.3 体验验收与之一一对应。

**[通过]** 高严重度 2（AI 业务契约）：3.4.1–3.4.8 八个能力均固化输入/输出字段、错误码、降级路径，并明确错误码命名规范（如 `TRIAGE_AI_TIMEOUT`、`RX_AUDIT_TIMEOUT`、`MR_GEN_OUTPUT_INCOMPLETE` 等），具备契约测试可执行性。

**[通过]** 中严重度 3（线下接诊业务覆盖）：新增 3.2.9 线下窗口、3.2.10 收费/退费；4.3.5 线下窗口流程；2.1 角色清单补全"线下接诊医生"；2.6 权限矩阵补全线下接诊医生 6 项可访问功能（线下挂号/退号、收费/退费、费用查询等）。

**[通过]** 中严重度 4（业务规则量化）：3.1.1 给出手机号正则 `^1[3-9]\d{9}$`、密码 6–20 位且含字母+数字、姓名 1–20 字、年龄 0–150；3.1.3.1 主诉 < 10 字提示 + 失败 3 次兜底；3.1.4.1 取消 ≥ 2h；3.2.2.2 主诉必填 ≥ 5 字、单字段 ≤ 2000 字；3.2.2.3 置信度 < 60% 提示、30s 超时；3.2.2.6 剂量上限告警；3.2.2.7 风险等级三档规则与 15s 超时；3.2.2.8 输入 < 50 字禁用 AI 生成。

**[通过]** 中严重度 5（健康档案实体）：5.1 新增"健康档案"实体，5.3 给出"患者 1—1 健康档案"关系，3.1.6 给出字段约束（每项 ≤ 200 字、不重名），2.6 矩阵纳入健康档案查看/维护访问性。

**[通过]** 中严重度 6（缴费记录实体）：5.1 缴费记录新增"关联单据标识（挂号/处方/检查/检验，可选）"字段；5.3 补"缴费记录 N—1 关联单据"；3.1.7 明确"创建入口"（医生端 3.2.10、线下窗口 3.2.9、取消/退药联动）；5.4.6 新增缴费记录状态机（待缴费/已支付/已退费/部分已退费/已取消）。

**[通过]** 中严重度 7（角色权限矩阵）：2.6 给出 7 类角色 × 28 个功能点的访问矩阵，覆盖患者端/医生端/管理端所有可识别的功能点，权限要求可被下游直接消费。

**[通过]** 中严重度 8（取消挂号循环引用）：3.1.4 与 3.1.4.1 已解除循环，3.1.4.1 给出完整规则集；4.2.3 给出可执行的取消挂号流程。

**[通过]** 中严重度 9（首页入口范围）：3.1.8 以表格明确 9 个入口的范围判定（"住院服务"为范围外且仅占位、"便民服务区"为范围内且为推断常用工具集合等），消除 v1 的范围模糊。

**[通过]** 低严重度 10（追溯矩阵粒度）：9 章已重写为条目级（REQ-001 ~ REQ-050），每条标注需求章节与主要来源文档简称（01/02/03），满足 requirement.md 第 5 条"可追溯性"要求。

**[通过]** 低严重度 11（异常分支"暂无"）：3.2.1 补叫号空态与终态、3.2.2.5 补 AI 解读加载与空态、3.2.4 补数据异常复核、3.2.5 补已发药退药边界、3.4.6 给出与 3.4.5 一致的完整契约，不再使用"暂无"或"与 3.4.5 类似"。

**[通过]** 低严重度 12（"AI 分诊台"边界）：3.3.3 顶部段落已明确 3.3.3（管理端规则配置入口）与 3.4.1（底层 AI 推理能力）的关系链，3.2.3/3.2.4 中"AI 分诊台"指代清晰。

**[通过]** 低严重度 13（实体状态机）：5.4 章节覆盖 6 个核心实体的状态机、触发条件与禁止流转规则；5.5 数据约束中明确"实体的状态变更必须严格遵循 5.4 中定义的状态机，后端需在数据层进行约束"。

**[通过]** requirement.md 原始任务要求的 7 项输出要求（纯需求文档 / 覆盖范围 / 详细程度 / 结构化呈现 / 可追溯性 / 不夹带技术决策 / 简体中文）均已满足：文档不包含任何具体技术栈名称，使用"后端系统""前端系统""数据库""AI 服务"等抽象术语；按"业务背景→角色与权限→功能需求→业务流程→数据需求→非功能需求→验收要求"组织；每项功能均覆盖业务目标、用户场景、关键交互、输入输出、异常分支；业务流程明确参与角色与阶段划分。

## 修改要求

无。
