# 技术方案审查报告（v4）

## 审查结果

APPROVED

## 逐维度审查

### 1. 技术准确性

**[通过]** 文档所引用的技术栈与组件均存在且适用：
- 后端：Spring Boot 3、Spring Security + JWT + BCrypt、Spring Data JPA、SQL-Toy（中文 ORM，用于复杂报表/统计）、Spring AI（ChatModel / Structured Output）、OpenFeign / Sleuth / Seata 占位、Nacos 服务注册与配置中心、Eureka、Spring Cloud Gateway — 全部为现有主流技术，与 Docs/02_tech.md 基线一致。
- 前端：Vue 3 + TypeScript + Vite + Element Plus + Pinia + Vue Router + Axios — 选型与原始技术栈文档一致。
- 中间件/协议：WebSocket（高风险告警）、SSE（病历流式输出）、HL7 / DICOM / 串口 / 网关（医疗设备协议） — 均为真实医疗信息化协议。
- 工具链：Knife4j（Swagger UI 增强）、Flyway / Liquibase（DB 迁移）、Logback + MDC（追踪号）、Sentry / 自建埋点（前端可观测性） — 真实存在。
- 持久化双栈（JPA + SQL-Toy）边界明确：JPA 用于 CRUD 主路径，SQL-Toy 用于复杂聚合（趋势图、排班统计、性能指标），与 Phase 0.3 决策一致。
- 性能指标未在路线图锁定具体阈值，Phase 6.2 明确"具体阈值由 OOD/性能阶段文档给出"——避免路线图越界为不可控的硬性承诺。

**[通过]** v3 阶段对 3.4.7（影像分析）Phase 4 推理 / Phase 5 管理面的职责切分已落地为正文表述（Phase 4.2 / 5.2 / 5.6），无错配。

**[通过]** AI 进阶底座"五组件"（凭证 / 路由 / Prompt 版本管理 / A/B 框架 / 埋点）口径在 Phase 5.2 / 5.3 / 5.5 / 5.6 全文统一，无"四/五"混用。

### 2. 完备性

**[通过]** v3 反馈的 12 项问题（3 P1 + 6 P2 + 3 P3）已全部落地为 v4 正文修订，无残留：
- P1-1（3.1.3.2 vs 3.4.8 时序）：Phase 2.2 / 2.4 / 2.5 / 5.2 / 5.3 / 5.4 / 5.5 / 5.6 全文同步；阶段间总览表 Phase 2 / 5 行同步反映；降级回退到 Phase 2 Mock 兜底文案。
- P1-2（取消挂号 vs 缴费记录时序）：Phase 2.2 / 2.3 / 2.4 / 2.6 统一为">2h 允许 → '已取消' + 预留'待退费'中间态字段（本阶段恒为 null）"，避免出现"已支付"歧义。
- P1-3（3.1.8 药房取药）：Phase 2.2 / 2.4 占位卡片 + Phase 4.2 / 4.4 / 4.5 真实数据接入（数据源 = 3.2.5 药房处方状态机"已发药（待取）"）。
- P2-1（3.1.5 病历/处方查询接入时点）：Phase 2.2 占位 + Phase 3.3 / 3.4 草稿可见 + Phase 4.2 / 4.4 完整版接入，5 类子项分项标注。
- P2-2（3.4.5 / 3.4.6 失败回退）：Phase 4.3 / 4.4 / 4.6 显式纳入"医生手动判读"降级路径与准出冒烟；Phase 6.1 非功能需求"可用性"项同步加注。
- P2-3（EXAMINATION 自动派单衔接）：Phase 2.3 引入 `assignment_status=PENDING_PHASE4_DISPATCH` 占位状态 + Phase 4.3 首次回填入队规则，时序闭环。
- P2-4（健康档案基础版/完整版边界）：Phase 1.2 新增独立"健康档案基础版边界声明（v4 收口）"段落——四要素（基础版范围 / 不含项 / `allergy_details` 前端录入 UI 同步提供 / 基础版与完整版分界点定义）显式声明。
- P2-5（Phase 5.6 准出计数）：13 项 AI 能力明确分项列出 6（Phase 4 落地）+ 7（Phase 5 落地）= 13，并显式声明"3.4.7 模型管理面"为既有项的扩展而非独立第 14 项。
- P2-6（任务六口径）：Phase 3.6 修订为"核心诊疗场景冒烟通过（医生端单端诊疗闭环）"；协作硬约束表同步更新为"Phase 3 + Phase 6 分两阶段收口"。
- P3-1（子任务包命名）：Phase 4.5 重排为 A / B / C / D / D1 / D2 / E / F / G，"包 B2"改名为"包 D"。
- P3-2（取消规则引擎描述）：与 P1-2 同步合并修订，Phase 2.2 显式包含"挂号状态机由'待就诊'推进为'已取消'"位置。
- P3-3（后端模块命名约定）：Phase 0.2 加注 + Phase 0.3 新增"后端模块命名约定"决策行，明确 `pharmacy` 含药房 + 药库子包、视 OOD 阶段规模评估是否拆分。

**[通过]** 需求覆盖完整：六大业务域（患者服务 / 门诊诊疗 / 检查检验 / 药房药库 / 医院管理 / AI 能力）、三大终端、5 个医生子岗位、13 项 AI 能力、核心 6 任务 + 拓展 6 任务、§5 数据需求、§6 非功能要求（性能 / 可观测性 / 安全 / 可用性 / 可维护性 / 兼容性）均在 Phase 6.1 非功能需求承接一览中显式承接。

**[通过]** 数据流形成闭环：
- 患者主流程：分诊（Phase 2）→ 挂号（Phase 2）→ 接诊（Phase 3）→ 检查/检验（Phase 4）→ 药房/退药/退费（Phase 4）→ 全链路联调（Phase 6）。
- 退费/退药链路：取消规则（Phase 2 占位 + '待退费'字段预留）→ 缴费记录状态机（Phase 4 落地）→ 退药角色矩阵（Phase 4）→ 线下窗口实际退费（Phase 4）。
- 药房取药：占位（Phase 2）→ 真实数据接入（Phase 4，数据源 = 3.2.5 处方状态机）→ 全链路联调（Phase 6）。
- AI 知识库：Mock 兜底（Phase 2）→ 真实 3.4.8 接入（Phase 5）→ 流式 / 持续优化（Phase 6）。
- 历史 EXAMINATION 占位申请：`PENDING_PHASE4_DISPATCH`（Phase 2）→ 首次回填入队（Phase 4）→ 即时派单（Phase 4 后续）。

**[通过]** 5 任务（任务六联调）的"两阶段收口"（Phase 3 单端诊疗闭环 + Phase 6 全链路联调）在 Phase 3.6、协作硬约束表、与其他文档的关系、阶段间总览表四处口径统一，无字面张力。

### 3. 可操作性

**[通过]** 每个阶段均含"阶段目标 / 阶段交付物 / 关键技术决策 / 可演示能力 / 可分配子任务包 / 阶段准入/准出"六要素，结构高度一致；子任务包粒度足以分配给不同开发者（Phase 0.5 列出 7 个模块、Phase 1.5 列出 4 个包、Phase 2.5 列出 5 个包、Phase 3.5 列出 7 个包、Phase 4.5 列出 8 个包、Phase 5.5 列出 7 个包、Phase 6.5 列出 6 个包）。

**[通过]** Phase 0 最小化骨架（7 类别）已收敛：工程结构 / 后端骨架 / 前端骨架 / 协作规范 / 持续集成 / 基础组件 / 部署骨架；多模块 Maven 聚合 + OpenAPI/TS 类型生成 + 共享类型包 + 共享 `common` 抽象层（OpenFeign / Sleuth / Seata 占位），明确支持"骨架完成后多人可并行贡献"硬约束。

**[通过]** 所有降级路径均给出明确触发条件与降级行为：
- 3.4.1 智能分诊：超时/3 次失败 → 兜底"建议直接联系线下接诊窗口"。
- 3.4.2 AI 处方审核：6 秒硬超时 → 本地规则校验 + "未经过 AI 审核"标签。
- 3.4.4 / 3.4.9（Phase 3 占位）：Mock 失败/超时 → 本地诊断规则表 / 检查项目字典 + "由本地规则生成"UI 标识。
- 3.4.5 / 3.4.6：AI 失败/超时 → "医生手动判读"模式 + "AI 不可用"标签 + 原始数据仍可录入落库。
- 3.4.7 模型管理面（Phase 5）：注册/授权/审计独立于推理逻辑。
- 3.1.3.2 病情咨询：3.4.8 不可用时回退到 Phase 2 Mock 兜底文案（"暂不可用，请稍后重试"），降级路径与 3.4.2 一致。
- 退药：被拒时状态机变更"已拒绝"、库存与缴费记录不变更、走原渠道告知。
- 调拨/盘库：双写对账失败事务回滚 + 差异分级告警。
- 硬件采集：Phase 4 仅落接口契约 + Mock 数据，真实协议分设备类型逐步接入。

**[通过]** 跨阶段时序约束已无歧义：
- 挂号状态机"待退费"中间态字段由 Phase 2 预留占位、Phase 4 缴费记录联动写入。
- 取消规则引擎"距就诊时间"判定基准（>2h 允许 / ≤2h 拒绝）。
- EXAMINATION 占位申请 `PENDING_PHASE4_DISPATCH` 状态字面定义 + Phase 4 首次回填入队规则。
- 3.1.5 病历/处方查询在 Phase 3 即可见草稿、Phase 4 接入完整版（分 5 类子项）。
- 3.1.3.2 在 Phase 2 仅 UI + Mock 兜底，Phase 5 升级为真实 3.4.8 调用；前端 disclaimer 跨阶段保留。

**[通过]** Phase 0.2 类别收敛为 7 类、Phase 0.3 后端模块命名约定显式（pharmacy 含药房 + 药库子包）、Phase 0.5 任务分配列出 7 个模块，骨架阶段"协作地形"足够支撑多人并行贡献。

**[通过]** 风险表覆盖了 v3 全部遗留点（健康档案边界 / SQL-Toy 遗漏 / §6 非功能需求 / Phase 0.2 类别数 / AI 底座口径 / 前向引用）以及 v4 新增点（3.1.3.2 时序 / 取消挂号时序 / 3.1.8 药房取药 / 3.4.5/3.4.6 降级 / EXAMINATION 派单衔接 / Phase 3.6 任务六张力 / 子任务包命名 / 后端模块命名），且每条均标注"v3/v4 已解决"。

## 修改要求

无。文档满足"决策明确、路径清晰、准确可信"的实现启动条件。
