根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

> 本轮审查报告（b_v10_diag_v2.md）已通过质询验证（b_v10_challenge_v2.md = LOCATED），证据充分、逻辑自洽、覆盖完整。请按以下问题清单逐项落实改进。

## 当前审查结果

### P0-1（主问题）：内容膨胀反向风险未实质解决 + 修订说明（v10）字节数事实错误

**主问题描述**：实测 v10 = 106,136 字节（764 行），距 v9 重置目标 ≤ 92,000 字节超出 14,136 字节（+15.4%）；即使按 v10 自述的 102,413 字节仍超 10,413 字节（+11.3%）。下挂 4 个子表现，需统一指向同一改进路径。

#### 子表现 1.2：v10 修订说明 L754 字节数事实错误（v2 新发现，严重程度：严重）

- **位置**：L754 修订说明（v10）
- **问题**：原文"v10 文件大小从 v9 的 103,058 字节降至 102,413 字节（-645 字节）"。实测 106,136 字节 ≠ 自述 102,413 字节，差 3,723 字节；若 v9 = 103,058 字节可信，则 v10 实际为 +3,078 字节增长（+3.0%）而非 -645 字节缩减。修订说明作为 v10 修订轨迹凭证，字节数事实错误会直接破坏文档可信度——首次阅读者用 `wc -c` 复核会发现与自述不符。
- **改进建议**：用 `Get-Item -LiteralPath "a_v11_tech_v1.md" | Select-Object Length` 或 `wc -c` 在 v11 完成后重新核测实际字节数；修订说明（v11）准确填写真实数值；若文件在说明写完后被追加内容，应回滚至说明对应字节数，或在说明中追加"说明写完后追加 X 字节（追加内容：……）"明确交代。

#### 子表现 1.3：13 项 AI 归口表三处仍逐项列示（v9 P1-3 部分未解决，严重程度：一般）

- **位置**：L484-L486（阶段间总览表 AI 列）、L500-L514（任务分层映射表核心任务三/四/五 + 影像 AI 行）、L527（协作硬约束表"13 项 AI 能力均提供降级路径"行）
- **问题**：附录 C（v9 收口新增）已具备 13 项 AI 落地阶段权威索引职能（L711-L732），三处重复列示属"内容膨胀反向风险"的具体落地表现。v10 修订说明 P1-3 自承"部分"未完成。
- **改进建议**：
  - 阶段间总览表 AI 列：删除逐项列示，改为"AI 列：见附录 C"；
  - 任务分层映射表 L500-L503 / L513-L514：删除具体 AI 编号（保留任务名 + Phase 归口），与附录 C 解耦；
  - 协作硬约束表 L527：降级路径行压缩为"13 项 AI 降级路径：见附录 C"指针，仅保留"覆盖阶段 Phase 2~5"。

#### 子表现 1.4：AI 进阶底座三处独立表述（v9 P1-6 部分未解决，严重程度：一般）

- **位置**：L383-L384（Phase 5.3 末段对照表）、L511（任务分层映射表"AI 进阶"行）、L528（协作硬约束表"AI 进阶底座职责分工"行）
- **问题**：L511 与 L528 两行仍各有完整措辞且与 L383-L384 略异（"流量分配机制" vs "流量分配"、"埋点采集" vs "推理性能埋点采集（不含基线与对比）"、"向量库索引维护与回滚" vs "向量库维护"）。三处措辞略异会让首次阅读者疑惑"是否指代不同事物"。
- **改进建议**：以 L383-L384（Phase 5.3 末段对照表）为唯一权威描述；L511 与 L528 两行改为单指针"详见 Phase 5.3 末段对照表"，删除具体措辞。

#### 子表现 1.5：修订说明 v6/v7/v8/v10 共 4 节 + 附录 A.1~A.3 + 附录 B 合计约 200 行（严重程度：一般）

- **位置**：L557-L613（附录 A.1~A.3）、L615-L653（附录 B）、L657-L671（修订说明 v6）、L697-L707（修订说明 v7）、L735-L743（修订说明 v8）、L747-L760（修订说明 v10）
- **问题**：约 200 行元信息占文档 26%——元信息密度过高，首次阅读者会被冲淡核心骨架内容。与"项目实现路线图"的核心职责（告诉阅读者"现在/下一步我该做什么"）关联弱。
- **改进建议**：
  - 附录 A.1~A.3（v1~v4 历次修订摘要）下沉至 `iteration_history.md`，文档仅保留指针"历次修订见 iteration_history.md"；
  - 修订说明 v6 / v7 / v8 同样下沉至 `iteration_history.md`；仅保留修订说明 v11 作为本轮修订记录；
  - 附录 B（已关闭风险）下沉至 `risk_register_closed.md`，仅保留指针。

**P0-1 总体目标**：v11 ≤ 95KB（约 95,000 字节）/ ≤ 750 行。

---

### P0-2（v2 新发现）：模块级接口契约冻结时机（Phase 0.6 Tier 2）颗粒度过粗（严重程度：严重）

- **位置**：Phase 0.6 L92（Tier 2 行）
- **问题**：原文"模块级端点（7 个业务模块的端点清单与 schema）——Phase 1 启动前冻结"，但 7 个业务模块的真正"消费方启动阶段"分布在 Phase 1~5：
  - patient = Phase 2 / doctor = Phase 3 / exam-lab-pharmacy = Phase 4 / admin = Phase 1+5 / ai-ability = Phase 2~5 / device-gateway = Phase 4
  - 强制 7 模块端点 Phase 1 启动前冻结意味着 exam/lab/pharmacy 业务规则需 Phase 1 启动前完成定义，与"分层递进"约束存在张力
  - 强制 ai-ability 全部 13 项 AI 抽象接口 Phase 1 前定型，会迫使 AI 抽象层定型过早
  - device-gateway 已声明为"软预备"，与 Tier 2 一刀切冻结要求存在内部张力
- **改进建议**：
  - Phase 0.6 L92 Tier 2 措辞修订为：**"模块级端点冻结时机 = 该模块首个消费方所在阶段启动前；多消费方模块（如 ai-ability）的端点清单按消费方所在阶段分阶段冻结——例如 ai-ability 模块的 3.4.1 抽象接口在 Phase 1 启动前冻结，3.4.2/3/10 抽象接口在 Phase 2 启动前冻结，3.4.4/5/6/7/9/11 抽象接口在 Phase 3 启动前冻结"**；
  - 同步在 Phase 0.5 任务分配段 A~G 后增加"**各模块端点冻结时点表**"：A = Phase 1 启动前 / B = Phase 2 启动前 / C = Phase 3 启动前 / D = Phase 1 启动前（基础部分）+ Phase 5 启动前（综合管理部分）/ E = Phase 1 启动前（3.4.1）/ Phase 2 启动前（3.4.2/3/10）/ Phase 3 启动前（3.4.4/5/6/7/9/11）/ F = 无业务端点 / G = Phase 3 启动前（软预备条目不要求冻结，仅占位）。

---

### P1-3（v2 新发现）：退药申请状态机无显式状态枚举（严重程度：一般）

- **位置**：Phase 4.3 L299-L303（退药角色矩阵）、Phase 4.2 L285（药房域 4 态实为处方发药状态机非退药申请状态机）
- **问题**：5 大状态机（缴费记录 5 态 / 调拨单 6 态 / 盘库单 4 态 / 处方状态机 4 态 / 挂号状态机 4 态）均有显式状态枚举；退药申请状态机仅在角色矩阵中通过"被拒兜底"间接提及"已拒绝"态，其余状态未固化，与其他 4 大状态机处理方式不一致。首次 OOD 阶段需先在路线图层固化状态枚举才能开工。
- **改进建议**：
  - 在 Phase 4.2 药房域行 / 4.3 退药角色矩阵段后追加显式状态枚举（参考调拨单 6 态格式）：**退药申请状态机 = 草稿 / 审批中 / 已通过 / 已执行（库存回补+缴费记录联动变更）/ 已拒绝 / 已撤销**；
  - 同步在 Phase 0.6 Tier 4 状态机契约清单中追加"退药申请状态机"行。

---

### P1-4（v2 新发现）：病历/处方"草稿" vs "完整版"边界未显式定义（严重程度：一般）

- **位置**：Phase 4.2 L290（真实数据接入行）
- **问题**：原文"①病历——Phase 3 起即可见草稿，本阶段接入完整版"，但全文无病历状态机定义，"草稿"与"完整版"分界点仅靠"Phase 3 AI 生成"与"Phase 4 真实检查/检验数据接入"时序隐式区分。同理"②处方——Phase 3 起即可见 AI 审核后草稿，本阶段接入完整版"——处方状态机仅在 Phase 4.2 L285 定义"待发药/已发药（待取）/已发药（已取）/已退药"4 态（侧重发药流转），未定义"草稿"vs"完整版"维度。患者端 3.1.5 可见性的真实时序依赖此定义。
- **改进建议**：
  - 在 Phase 0.6 Tier 4 状态机契约清单中追加"病历状态机"行——建议为：**病历生成中（AI 输出未确认） / 医生确认中（已编辑未保存） / 已保存（医生保存，患者端可见"草稿"） / 完整版（Phase 4 接入检查/检验数据后状态） / 已修订（医生修订后版本）**；
  - 处方状态机扩展为：**AI 审核中 / 医生确认中 / 已提交（已记账） / 已发药（待取） / 已发药（已取） / 已退药**（与现有 4 态融合为 6 态）。

---

### P2：正文中残留"v8 收口" / "v6 重命名"等历史标签（严重程度：轻微）

- **位置**：
  - Phase 0.6 L93：**软预备条目总览（原独立小节 0.7 已合并入 0.6）**——含"v8 合并"历史标签
  - 协作硬约束表 L526：**Phase 3 已将原"包 D（核心 2 + 核心 3 合并）"按可分配性硬约束拆分为 D-AI1（AI 处方审核 3.4.2）/ D-AI2（AI 病历生成 3.4.3）**——含"v6 重命名"历史标签
- **问题**：违反 v9 P1-1 / v8 P1-3 归一化要求（"移除正文中所有 v5~v8 收口/修订/重命名/简化/压缩/新增标签，直接陈述当前结论"）。
- **改进建议**：
  - Phase 0.6 L93 改为：**软预备条目总览**——删除"（原独立小节 0.7 已合并入 0.6）"括注；
  - 协作硬约束表 L526 改为：**每个阶段可拆解为可分配给不同开发者的子任务包 | 每个 Phase 子任务包对应单一职责或单一 AI 能力；D-AI1（AI 处方审核 3.4.2）/ D-AI2（AI 病历生成 3.4.3）以与 Phase 4.5 的 D1/D2（药库 / 调拨）解耦命名 | 全部**——删除"Phase 3 已将原'包 D'按可分配性硬约束拆分"历史叙述。

---

## 历史迭代回顾

### 已解决的问题（历史轮次提及但本轮不再出现）

- **v9 P0-1（L377 3.4.11 工作台分诊台术语冲突）**：v10 已修复——L373 改为"与 3.4.1 智能分诊的 Prompt 注入、与 3.4.11 执行顺序推荐的检查队列规则均相互独立"，与全文 13 处 3.4.11 定义闭环；v8/v9 连续 3 轮未修的尾巴已切断。
- **v9 P1-1 / P1-2（L52 / L56 元叙事段落 + OOD 兜底声明重复）**：v10 已删除 L52 / L56 两段引用块；OOD 兜底声明仅保留在文末修订说明。
- **v9 P1-4（5 处英文 OOD 命名残留）**：v10 已替换 exam_application / assignment_status / comorbidities / severity / disclaimer 五处为行为契约描述。
- **v9 P1-5（Phase 5.6 准出口径与附录 C 严重重复）**：v10 已精简 Phase 5.6 准出口径为 2 行指针。
- **v8 P2-3 / P2-4（协作硬约束表 / 风险表过长）**：v10 已分别从 15→5 行、19→5 行。
- **v7 P2-6（3.4.11 术语冲突）**：v10 已修复（同 v9 P0-1）。
- **v6 之前的核心三件套编号顺序、Phase 5.1 矛盾、退药角色矩阵基础结构、3.1.6 健康档案分层、3.1.7 缴费记录承接、3.4.x 降级路径、3.1.8 患者首页入口、5.4.1 EXAMINATION 占位申请衔接等**：均已在 v3~v8 各轮逐步收口。

### 持续存在的问题（多轮反复出现，本轮需重点解决）

- **内容膨胀反向风险（用户警示点 #11）**：
  - v7 P1-2：v7 反向加剧（101,545 字节，未减反增）
  - v8 P1-2：v8 仍 103,094 字节，v9 目标 ≤ v7 = 101,545
  - v9 P0-2：v9 = 103,058 字节，距目标差 1,513 字节
  - **v10 不仅未达成 v9 重置目标 ≤ 92,000 字节，反而增至 106,136 字节（+3,078 字节），且修订说明事实错误（声称 -645 字节）破坏可信度**
  - **本轮必须实质性压缩至 ≤ 95KB**（结合 P0-1 子表现 1.3/1.4/1.5 联动执行）
- **13 项 AI 归口表多处重复（v8 P2-5 → v9 P1-3 → v10 P0-1.3）**：v8 提出合并附录 C，v9 新增附录 C 但三处源头未压缩，v10 仅 Phase 5.6 精简；**本轮必须执行 v9 P1-3 完整修复，三处统一改为附录 C 指针**。
- **AI 进阶三处独立表述（v9 P1-6 → v10 P0-1.4）**：v9 已识别，v10 仅 Phase 5.6 加指针；**本轮必须执行 v9 P1-6 完整修复，以 L383-L384 为唯一权威**。
- **历史标签归一化（v7 P2-8 → v8 P1-3 → v9 P1-1 → v10 P2）**：v9 已删除 L52/L56，v10 仍保留 Phase 0.6 L93 与协作硬约束表 L526 两处；**本轮必须彻底清除**。

### 新发现的问题（v11 新识别）

1. **P0-1.2 v10 修订说明 L754 字节数事实错误**：实测 106,136 ≠ 自述 102,413（差 3,723 字节）——v1~v9 历次修订说明均未出现此类事实错误，需在 v11 修订说明中谨慎核实。
2. **P0-2 Tier 2 颗粒度过粗**：v10 引入 Tier 1~4 4 档接口契约冻结时机框架（v10 修订说明的关键改进），但 Tier 2 措辞与"分层递进"约束存在内在张力——v1~v9 历次审查均未识别此问题（因为 4 档框架是 v10 才引入的新结构）。
3. **P1-3 退药申请状态机无显式状态枚举**：v3/v4 轮次审查关注退药角色矩阵的完整性，但未明确要求状态枚举；v11 对照其他 4 大状态机的处理方式后识别此一致性缺口。
4. **P1-4 病历/处方草稿 vs 完整版边界**：v3 P1-4 已关注 3.1.5 真实数据接入，v10 通过"Phase 3 起即可见草稿，本阶段接入完整版"措辞响应，但路线图层未固化病历状态机；v11 识别此为状态机定义完整性问题。

---

## 改进优先级与执行路径

| 优先级 | 问题 | 严重程度 | 关键执行动作 |
|--------|------|---------|-------------|
| **P0-1（主问题）** | 内容膨胀反向风险（含 1.2 事实错误、1.3~1.5 三个子表现） | 严重 | (a) v11 完成后用 `Get-Item .Length` 重测字节数，修订说明（v11）准确填写；(b) 三处 13 项 AI 归口表统一改为附录 C 指针；(c) AI 进阶三处统一以 L383-L384 为权威；(d) 附录 A.1~A.3 / 修订说明 v6/v7/v8 下沉至 iteration_history.md，附录 B 下沉至 risk_register_closed.md；**目标 v11 ≤ 95KB / ≤ 750 行** |
| **P0-2（新发现）** | Tier 2 冻结时机颗粒度过粗 | 严重 | Phase 0.6 L92 Tier 2 措辞改为按消费方所在阶段分阶段冻结；Phase 0.5 同步增列"各模块端点冻结时点表" |
| **P1-3（新发现）** | 退药申请状态机无显式状态 | 一般 | Phase 4.2/4.3 追加显式 6 态枚举（草稿/审批中/已通过/已执行/已拒绝/已撤销）；Phase 0.6 Tier 4 同步列入 |
| **P1-4（新发现）** | 病历/处方"草稿 vs 完整版"边界 | 一般 | Phase 0.6 Tier 4 增列病历状态机；处方状态机扩展为 6 态融合 |
| **P2** | 正文中残留"v8 收口" / "v6 重命名"标签 | 轻微 | Phase 0.6 L93 删括注；协作硬约束表 L526 删历史叙述 |

---

## 保留 v10 已达成的关键质量收益（不可回退）

以下改进项在 v10 已达成，v11 必须保留：

- v9 P0-1（3.4.11 术语冲突）已修复——v9 连续 3 轮未修的尾巴已切断
- Phase 0 整段已压缩至 72 行（达 v8 P1-2 目标上界 60-70 行附近）
- 5 处英文 OOD 命名替换（exam_application / assignment_status / comorbidities / severity / disclaimer）已完成
- 协作硬约束表 15→5 行、风险表 19→5 行
- v9 P1-1 / P1-2 元叙事段落已删除
- Phase 0.6 Tier 1~4 4 档接口契约冻结时机框架（v10 引入的关键改进结构，仅需在 Tier 2 措辞上修订，框架本身保留）
- 6 大业务域、3 大终端、医生 5 子岗位、13 项 AI、3 大核心任务、6 项拓展任务、5 大状态机、§6 非功能需求、AI 进阶五组件、持久化双栈的全量显式承接（覆盖完整性已达良好）

---

## 上一轮产出路径

C:\Develop\Software\AIMedicalSys\Harness\redeliberations\202606150151_项目实现路线图设计\a_v10_tech_v1.md

## 用户需求

C:\Develop\Software\AIMedicalSys\Harness\redeliberations\202606150151_项目实现路线图设计\requirement.md
