# 再审议判定报告（v6）

## 判定结果

RETRY

## 判定理由

1. **诊断报告质量问题分布**：组件B v2 诊断报告识别出 6 项实质性问题，按严重度分类：
   - **严重问题 1 项**：S-2（Phase 0 提交规范与 CI 门禁"文件名占位"无实质内容，直接违反 requirement.md §2 第 4 点"骨架完成后必须支持多人并行贡献"硬约束与 §7 第 2 条"新加入的开发者能基于骨架文档独立开始工作"验收隐含标准）
   - **一般问题 4 项**：S-1（接口契约"已冻结"措辞可能误导下游对冻结粒度的判断，违反 §2/§7 多人并行硬约束的"软"侧面）、S-3（Phase 0 骨架"硬必需"条目数 7 项中 1-2 项可下沉，违反 §2 骨架最小化硬约束）、P2-3（协作硬约束表精简后"任务分层覆盖"归口缺失，违反 §4.6 任务分层维度）、P2-4（路线图层面渗入 OOD 级别命名，违反 §5 不写 OOD 边界）

2. **质询报告结论**：组件B v2 质询报告对 v2 诊断报告给出 **LOCATED**（已定位）判定——证据充分性、逻辑完整性、覆盖完备性、报告必要性四个维度全部通过，确认诊断报告准确识别了 6 项实质性问题，并明确指出 v6 文档作者可据此在 v7 中重点修复 S-2（必修）+ S-1（必修）+ P2-3（必修）+ S-3（应修）+ P2-4（应修）+ P2-1（可修）。

3. **循环轮次**：组件B内部循环实际轮次 2 < 最大轮次 12，质询报告 LOCATED 而非 CHALLENGED，说明审查结论已被确认，无循环耗尽。

4. **判定依据**：根据判定标准"RETRY（重新运行）：审查报告包含严重或一般等级的问题"——本轮诊断报告包含 1 项严重问题（S-2）与 4 项一般问题（S-1、S-3、P2-3、P2-4），且质询报告已 LOCATED 确认这些问题的真实性，必须触发组件A重新运行以修复这些问题。

## 需要解决的问题

- **问题描述**：Phase 0.2 协作规范行仅列举"仓库 README、CONTRIBUTING、AGENTS.md、分支模型、Commit 规范、PR 模板、Code Review 清单"等文件名占位，无任何具体内容或示例。新加入开发者读完 Phase 0 文档后仍不知道分支该如何命名、Commit message 格式是什么、PR 模板包含哪些字段、CR 清单有哪些必查项。工具选型下沉到 OOD 是合理的，但流程级规范（分支命名、Commit 格式、PR 模板字段、CR 检查项）与工具选型无关，应在 Phase 0 落地。当前 Phase 0.2 协作规范行实质上是"占位文件名清单"，不是"可执行规范"，直接违反 requirement.md §2 第 4 点"骨架完成后必须支持多人并行贡献"硬约束与 §7 第 2 条"新加入的开发者能基于骨架文档独立开始工作"验收隐含标准
- **所在位置**：`a_v6_tech_v1.md` L44（Phase 0.2 协作规范行）
- **严重程度**：严重
- **改进建议**：
  - Phase 0.2 协作规范行追加最小化流程规范：分支命名格式（`feature/<TICKET-ID>-<short-desc>` 等）、Commit message 格式（参考 Conventional Commits）、PR 模板字段（变更范围/影响模块/接口契约变更/测试覆盖/截图）、CR 必查项（接口契约是否变更/是否有迁移 SQL/是否更新文档）
  - CI 门禁增列"必过项清单"：构建 + 单测 + 接口契约校验（OpenAPI diff）+ 代码风格（具体工具 OOD 选型）

- **问题描述**：Phase 0.2 工程结构行写"接口契约骨架……作为工程结构层面交付的一部分"，措辞并列"接口契约骨架"与"工程结构层面交付"可能让下游误读为"接口契约已在 Phase 0 冻结到可分配粒度"。但实际上 Phase 0 仅冻结了框架级规范（统一响应、分页、错误码命名空间），7 个模块的端点 schema 未冻结时机。下游可能误读导致多人并行贡献时产生接口协商阻塞
- **所在位置**：`a_v6_tech_v1.md` L41（Phase 0.2 工程结构行）
- **严重程度**：一般
- **改进建议**：
  - Phase 0.2 增列"接口契约冻结时机表"：框架级（统一响应/分页/错误码/3.4.x 占位类型）= Phase 0 冻结；模块级（7 个模块的端点清单与 schema）= Phase 1 启动前冻结
  - 措辞由"接口契约骨架作为工程结构层面交付的一部分"改为"接口契约骨架（**框架级**）作为工程结构层面交付的一部分；模块级接口契约冻结时机详见 §2 多人并行硬约束三要素核查表"

- **问题描述**：Phase 0.2 硬必需条目数为 7（工程结构 / 后端骨架 / 前端骨架 / 协作规范 / 持续集成 / 基础组件 / 部署骨架），其中 1-2 项可下沉到 Phase 1：(a) 持续集成（CI 占位流水线）在 Phase 1 第一个 PR 出现前不强制需要；(b) Phase 0.7 预备性占位子节与 Phase 0.2 / 0.3 / 0.5 在"层级"列与软预备条目上存在结构性冗余。违反 requirement.md §2 第 3 点"骨架阶段是整个流程的第一步，且应最小化"硬约束
- **所在位置**：`a_v6_tech_v1.md` L45（Phase 0.2 持续集成行）+ L93-L103（Phase 0.7）
- **严重程度**：一般
- **改进建议**：
  - Phase 0.2 持续集成行"层级"列由"硬必需"改为"软预备"；CI 必过项清单作为 Phase 1 首期交付
  - Phase 0.7 整段下沉到 Phase 0.6 准出条款的最后一段（"软预备条目按'已固化位置/已引入依赖'为达成标志"），或保持 0.7 但删除与 0.2/0.3/0.5 的字段重复（仅保留 0.7 的"补齐时机"列）

- **问题描述**：v6 修订说明将协作硬约束表从 15 行精简至 5 行，但"任务分层覆盖"行被一并删除，导致 requirement.md §4 第 6 项任务分层维度要求的"核心任务一～六（基础设施 + 患者端 + 分诊 + 审核 + 病历 + 联调）→ 拓展任务（实时通信/微服务/AI 进阶/部署/硬件采集/影像 AI）"在 v6 全文中无任何独立章节将"核心任务一/二/三/四/五/六"与具体 Phase 形成一对一映射表，6 项拓展任务的 Phase 归口也无独立映射表
- **所在位置**：协作硬约束表 L502-L508（精简后缺失任务分层覆盖行）；全文无任务分层独立映射表
- **严重程度**：一般
- **改进建议**：
  - 在阶段间总览对照表后新增独立"任务分层 ↔ Phase 映射表"小节（建议 5-6 行），列出核心任务一～六与 6 项拓展任务的 Phase 归口
  - 推荐采用此方案，不破坏 v6 的"协作硬约束表精简"收口成果

- **问题描述**：v6 中出现大量 OOD 级别命名——提供者/服务类名（`TriageRuleProvider` / `ExamRecommendRuleProvider` / `AIService`）、数据字段名（`allergy_history` / `allergy_details` / `reaction_type` 等）、状态枚举值（`PENDING_PHASE4_DISPATCH`）、表名（`exam_application`）、API 路径（`api/triage/consult` / `knowledge/qa`）、抽象基类（`Result<T>` / `GlobalExceptionHandler` / `BaseEntity`）共 30+ 处。虽部分命名可作为"跨阶段协作契约"在路线图层面冻结，但已涉及到类、字段、状态枚举值、表名级别，违反 requirement.md §5 边界"不写 OOD 类图、不定义具体类、不写方法签名、不写接口字段表"及 §2"比 OOD 文档更上层、更笼统"要求
- **所在位置**：多处，约 30+ 处（Phase 1.2 / 2.2 / 2.3 / 3.3 / 4.2 / 4.3 / 5.3 等）
- **严重程度**：一般
- **改进建议**：
  - 路线图层面仅保留"行为契约"描述，类名/字段名/状态枚举值/API 路径/抽象基类下沉至 OOD 阶段
  - 仅下沉命名本身（如 `TriageRuleProvider` → "分诊规则降级提供者"），保留"行为契约"描述（如"分诊降级：AI 超时回退到本地规则表"）
  - 具体替换示例：`PENDING_PHASE4_DISPATCH` → "Phase 4 派单前的占位状态"；`api/triage/consult` → "分诊咨询接口"；`Result<T>` → "统一响应包装"；`BaseEntity` → "实体基类"