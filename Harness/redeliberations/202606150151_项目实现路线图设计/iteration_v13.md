# 再审议判定报告（v13）

## 判定结果

RETRY

## 判定理由

### 1. 诊断报告问题等级分析

组件B v2 诊断报告（`b_v13_diag_v2.md`）识别出 17 个问题：

| 问题编号 | 主题 | 等级 |
|----------|------|------|
| 问题 1 | 3.4.3 AI 病历生成的降级路径缺失（13 项 AI 降级完整性漏洞） | 一般 |
| 问题 2 | 3.4.7 影像分析在 Phase 4 的"模型来源"歧义（实施歧义漏洞） | 一般 |
| 问题 3 | 协作硬约束表 L547 措辞与附录 C 实际内容不匹配（协同关系漏洞） | 一般 |
| 问题 4 | v8 P1-1 兜底声明"协作便利锚点"在 v13 中已删除（OOD 边界条款回退） | 一般 |
| 问题 5 | Phase 4 关键路径"路径 3+4 串行 4 包"工作量不均衡 | 一般 |
| 问题 6 | Phase 4.5 包 C + Phase 5.5 包 G 颗粒度过粗 | 一般 |
| 问题 7 | Phase 4.5 D 命名风格与 Phase 3.5 D-AI1/D-AI2 不一致 | 一般 |
| 问题 8-17 | 修订说明措辞、命名残留、状态机判定标准等 10 项 | 轻微 |

- **严重等级问题**：0 项
- **一般等级问题**：7 项
- **轻微等级问题**：10 项

### 2. 质询报告分析

组件B v2 质询报告（`b_v13_challenge_v2.md`）结果为 **LOCATED**（审查被确认），质询从证据充分性、逻辑完整性、覆盖完备性、报告必要性 4 个维度全部通过——v2 报告作为组件B 内部产物已准确识别组件A 产出的质量问题，组件A 可据此改进。

### 3. 内部循环状态

- 最大轮次：12
- 实际轮次：2
- 终止原因：v1 报告严重偏离任务焦点（v1 质询指出），v2 报告重置审查焦点后被质询确认（LOCATED）——属"提前终止 + 审查被确认"模式，循环未耗尽。

### 4. 判定依据

按判定标准：
- "审查报告包含严重或一般等级的问题" → **RETRY**

诊断报告含 7 项一般等级问题（其中 4 项被 v2 报告 Section 6 审查结论定性为"实质性漏洞"：3.4.3 降级路径缺失 / 3.4.7 模型来源歧义 / v8 P1-1 兜底声明回退 / L547 措辞不匹配），均直接影响产出完整度与准确性（非致命但需修复）。

组件A v13 产出需在 v14 优先修复这 4 项实质性漏洞 + 3 项一般问题（Phase 4 路径工作量均衡 / 任务包颗粒度 / 命名一致性）+ 1 项系统性质量信号（字节数核测流程嵌入）后，再以"正式实施版"对外发布。

## 需要解决的问题

按 v2 诊断报告 v14 强制改进清单的 P1 优先级（实质性漏洞）：

- **问题描述**：13 项 AI 降级完整性漏洞——3.4.3 AI 病历生成在 AI 调用失败/超时时回退模式未声明，Phase 3 医生端诊疗闭环在 3.4.3 不可用时无兜底形态
- **所在位置**：Phase 3.2 L237 AI 能力行"核心三件套之核心 3 [AI 病历生成]"
- **严重程度**：一般
- **改进建议**：在 L237 增列降级路径——方案 A：AI 失败/超时时退回到"医生手动填写病历字段"模式；方案 B：退回到"AI 生成空白模板 + 医生补全"模式。Phase 3.6 准出口径同步增列"3.4.3 降级路径冒烟通过"

- **问题描述**：3.4.7 影像分析在 Phase 4 的"模型来源"歧义——"岗位决定可用模型"但模型管理面（注册/授权/审计）是 Phase 5 才扩展，Phase 4 期间"可用模型"从何而来未明示
- **所在位置**：Phase 4.2 L304 检查域行
- **严重程度**：一般
- **改进建议**：L304 增列"Phase 4 期间 3.4.7 推理的'可用模型'来源 = `device-gateway` 提供的预置模型列表（无注册/授权/审计；Phase 5 模型管理面扩展时迁移到管理面表）"；风险登记册附录 D 增列过渡期一致性风险

- **问题描述**：v8 P1-1 兜底声明"协作便利锚点"在 v13 中已删除，路线图与 OOD 边界条款实质性回退；v13 中残留约 5-7 处 OOD 命名无任何路线图层面的边界声明保护
- **所在位置**：v13 文档全局（兜底声明缺失 + L247 / L317 / L331 / L391 / L425 / L546 等位置命名残留）
- **严重程度**：一般
- **改进建议**：v14 阅读指引"文档边界"段补 1-2 句兜底声明"本文档中出现的具体类名/字段名/状态枚举值/API 路径仅作'协作便利锚点'使用——OOD 阶段首次评审重审全部命名"；抽样核查 5-7 处 OOD 命名按行为契约描述替换

- **问题描述**：协作硬约束表 L547 措辞"13 项 AI 降级路径：见附录 C"与附录 C L737-L749 实际内容不匹配——附录 C 无"降级路径"列，属"过度收口"
- **所在位置**：协作硬约束表 L547
- **严重程度**：一般
- **改进建议**：L547 改为"13 项 AI 降级路径：见各阶段关键技术决策段（按'首次落地阶段'分散声明；首次落地阶段定义见阅读指引）"

- **问题描述**：Phase 4 关键路径"路径 3+4 串行 4 包"工作量不均衡——路径 3+4 串行 1 位开发者承担 4 个包（C / D1 / D2 / E），约需 8-12 周，是 Phase 4 演示能力的关键路径瓶颈
- **所在位置**：Phase 4.1 关键路径与里程碑小段 L286-L297
- **严重程度**：一般
- **改进建议**：方案 A（推荐）：包 C 拆分为 C1（药房域基础 + 发药 + 药房库存扣减/回补事件）+ C2（退药角色矩阵 + 退药状态机 6 态 + 退药事务边界）；方案 B：路径 3+4 串行 1 位 → 2 位（包 C/D1/D2 共 1 位 + 包 E 独立 1 位）

- **问题描述**：Phase 4.5 包 C 含 4 个组件 + Phase 5.5 包 G 含 5 个组件，颗粒度过粗；Phase 4.5 D 命名风格与 Phase 3.5 D-AI1/D-AI2 不一致
- **所在位置**：Phase 4.5 L356 包 C、Phase 5.5 L425 包 G、Phase 4.5 L357 包 D
- **严重程度**：一般
- **改进建议**：包 C 拆 C1 / C2；包 G 拆 G1（LLM 凭证 + 多模型路由 + Prompt 模板版本管理）+ G2（A/B 框架 + 推理性能埋点）；Phase 4.5 D → D-AI 命名一致性
