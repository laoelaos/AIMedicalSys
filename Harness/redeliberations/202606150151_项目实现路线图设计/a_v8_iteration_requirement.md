根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### P1 级问题（必须在下一轮修复）

**P1-1：v6 的 P2-4 "OOD 边界渗透"未真正解决，"约定段落"封口失败**
- v7 修订说明 L728 已承认"未做 30+ 处命名的逐一下沉"，仅在 Phase 0.3 前用一段"L65 路线图层面 OOD 级别命名约定"封口。该方案存在三个根本问题：
  - (1) 约定不强制，下游 OOD 工程师仍会被命名误导，把"占位 + Mock"理解为"接口定型"
  - (2) 示例性命名引发固化认知（`TriageRuleProvider` / `PENDING_PHASE4_DISPATCH` / `allergy_details` / `api/triage/consult` / `Result<T>` 等约 30+ 处散布在 L42 / L61 / L77 / L184 / L193 / L242 / L326 / L386 / L388 等位置）
  - (3) 约定段落本身是"OOD 边界元说明"，反而把 OOD 边界的讨论内化进路线图
- 改进建议（方案 A 推荐 / 方案 B 兜底）：
  - 方案 A：真实下沉——具体类名替换为"行为契约描述"（如 `TriageRuleProvider` → "分诊规则降级提供器"；`PENDING_PHASE4_DISPATCH` → "Phase 4 上线前的占位状态"；`api/triage/consult` → "分诊咨询接口"；`Result<T>` → "统一响应包装"；`BaseEntity` → "实体基类"）；具体字段名替换为语义描述；具体状态枚举值替换为语义占位
  - 方案 B：删除 L65 约定段落，在文末附"v8 收口说明"声明：路线图层面保留具体 OOD 命名为"协作便利"，要求 OOD 阶段在第一次评审时重审全部命名
  - 顺带：L65 整段应删除

**P1-2：内容膨胀风险反向加剧，Phase 0 子结构膨胀最严重**
- 文档从 v6 ~99KB 增长到 v7 101,545 字节（571+ 行），未减反增
- Phase 0 从"0.1~0.6"扩展为"0.1~0.7"七个层级，单阶段占 L29-L116 共 88 行（其余阶段平均 70-90 行）
- Phase 0 内部包含 4 张表（0.2 交付物 + 0.3 关键技术决策 + 接口契约冻结时机表 + 0.7 预备性占位）+ 6 段长说明
- "骨架阶段必须最小化"硬约束正被反向侵蚀
- 改进建议：
  - (1) 接口契约冻结时机表下移到"阅读指引"或附录
  - (2) 删除 L65 OOD 命名约定段落
  - (3) Phase 0.7 预备性占位小节与 0.2/0.3 的"层级"列重复，0.7 节只需保留"补齐时机"索引表
  - (4) Phase 0.6 准出条款的"软预备条目按已固化位置/已引入依赖为达成标志"行已在 0.7 节说明，0.6 节可移除
  - 期望效果：Phase 0 整段控制在 60-70 行

**P1-3：核心任务六的 Phase 归口有歧义，任务分层映射表与正文存在张力**
- v7 新增"任务分层 ↔ Phase 映射表"（L511-L535）将核心任务六（联调）归口为 Phase 3 + Phase 6
- 但 Phase 3.6 准出与 Phase 6.6 准出对此表述不一致；当前 v7 把"任务六"拆分为"单端诊疗闭环"与"全链路联调"两个变体但都简称为"核心任务六（联调）"
- 改进建议（方案 A / 方案 B 二选一）：
  - 方案 A：显式拆分——"核心任务六-a（单端诊疗闭环联调）"和"核心任务六-b（全链路联调）"两个子任务
  - 方案 B：显式说明累积关系——保持单一"核心任务六（联调）"但显式声明"联调的可演示范围在 Phase 3 是医生端单端、在 Phase 6 是全链路"

### P2 级问题（应在下一轮修复）

**P2-1：阅读指引"落地阶段定义"段落与"任务分层 ↔ Phase 映射表"信息重复**
- 阅读指引 L25 列出 13 项 AI → Phase 归口的逐项列表
- 任务分层映射表 L535 影像 AI 行又重复列 3.4.7 的 Phase 归口
- 改进建议：阅读指引精简为 1-2 句"详见'任务分层 ↔ Phase 映射表'"，删除逐项列表；或删除任务分层映射表的"影像 AI"行

**P2-2：Phase 4.5 子任务包命名风格与 Phase 3.5 不一致**
- Phase 3.5 用 D-AI1 / D-AI2 前缀命名
- Phase 4.5 用包 D / 包 D1 / 包 D2（无前缀）
- 改进建议：两种方案任选其一：
  - 方案 A：Phase 4.5 包 D 也加前缀，改为"包 D-AI"（3.4.4 / 3.4.9 真实 AI 推理承接）
  - 方案 B：Phase 3.5 去掉前缀，D-AI1 / D-AI2 改回 D1 / D2，但需在协作硬约束表或附录 B 加注"Phase 4.5 的 D1 / D2 含义不同，跨阶段引用需带阶段前缀"

**P2-3：Phase 5.3 L388 出现未在需求文档中定义的术语"3.4.11 工作台分诊台"**
- L388 写"3.4.11 工作台分诊台"，但 v7 全文其他位置 3.4.11 都被定义为"执行顺序推荐"
- 改进建议：
  - 如果是笔误：改为"AI 分诊台规则与 3.4.1 智能分诊的 Prompt 注入相互独立（不可混用）"
  - 如果是有意引入新概念：在 Phase 5.1 / 5.2 / 5.5 显式承接并定义与 3.4.1 / 3.4.11 的关系

**P2-4：Phase 0.2 工程结构行"7 个业务模块"列举与 Phase 1 / Phase 4 真实模块列表存在不一致风险**
- Phase 0.2 L42 后端骨架写 `common` / `gateway` / `auth` / `patient` / `doctor` / `exam` / `lab` / `pharmacy` / `admin` / `ai-ability` 等模块（10 个）
- L57 接口契约冻结时机表只列 7 个业务模块
- 改进建议：在 L57 显式声明"7 个业务模块（含 `common` / `gateway` / `auth` 基础模块共 10 个后端模块）"

**P2-5：v6 / v7 修订标签产生历史噪声，未做归一化处理**
- v7 至少 15+ 处"v5 收口"、"v6 收口"、"v6 修订"、"v7 收口"等历史轮次标签
- 改进建议：移除正文中所有"v5 收口"、"v6 收口"、"v7 收口"等标签，改为直接陈述当前结论；保留附录 A / B / D 中的"v1 → v2"等历史修订表

### P3 级问题（可选修复）

**P3-1**：附录 B 末段"v5 收口"标注与正文标签不对齐，建议在每个条目末加正文标签引用

**P3-2**：阶段间总览对照表 Phase 5 行"3.4.7（管理面扩展，非首次落地）"措辞与 L379 措辞重复且轻微不一致，建议统一为"3.4.7 仅在 Phase 5 扩展模型管理面，分析推理已在 Phase 4 落地，不纳入首次落地计数"

**P3-3**：Phase 6.1 L444 列出的"13 项 AI 降级路径声明"未与协作硬约束表 / Phase 5 等阶段同步核查，建议 L548 改写为完整映射表

**P3-4**：Phase 0.3 L77 "AIService 内部接口" 与 L65 OOD 命名约定自相矛盾，建议 L77 改为"对外暴露统一的 AI 抽象接口（具体类名 / 方法签名在 OOD 阶段定型，Phase 0 仅声明抽象层存在）"

### 本轮 v7 新引入的问题

**N1**：Phase 0.6 准出条款（L104）与 0.7 预备性占位小节（L106-L115）存在结构性重复——0.7 已声明"已固化位置/已引入依赖"达成标志，0.6 又重列相同条目。改进建议：0.6 仅声明"软预备条目按 0.7 节补齐时机"，不重列具体条目

**N2**：修订说明（v6）表头与修订说明（v7）表头未对齐，iteration_history 与 v6 修订说明的反馈计数不一致。改进建议：在 iteration_history.md 或附录 A 中做一次计数对账

**N3**：Phase 6.2 L456 性能行"性能指标统计查询使用 SQL-Toy"与 0.3 / 4.2 / 5.2 多处 SQL-Toy 措辞冗余。改进建议：统一为"复杂报表/统计查询使用 SQL-Toy"，并归并到 Phase 0.3 的统一声明

### 审查结论

v7 整体可接受，但**必须修复 P1-1 / P1-2 / P1-3 三项严重问题后方可作为定稿**。P1-1（OOD 边界渗透）是路线图定位的根本性风险；P1-2（内容膨胀反向加剧）是用户特别警惕的风险点；P1-3（核心任务六归口歧义）是历史 v3 P2-9 的反向回潮。

可选的结构性优化：把附录 A / B / D 拆为独立文件（如 `Docs/03b_roadmap_changelog.md`），让主文档更聚焦"路线图本身"，可能带来 15-20% 的正文瘦身。

## 历史迭代回顾

v6 反馈的 5 项问题在 v7 中的处理状态：
- **v6 P1（Phase 0.2 协作规范只有文件名占位）**：v7 L44 已闭环，实质性改进（分支命名 / Commit 格式 / PR 模板 / Code Review 必查项 / 配套文件清单）。✓
- **v6 P2-1（接口契约冻结粒度模糊）**：v7 新增"接口契约冻结时机表"（L52-L61），分 4 档明确。✓
- **v6 P2-2（骨架最小化违反）**：v7 把持续集成从"硬必需"移到"软预备"。**部分闭环**（v6 建议的"1-2 项下沉"只做了 1 项）。当前 v7 P1-2 显示 Phase 0 仍存在 88 行结构密度问题，与本项相关。
- **v6 P2-3（任务分层覆盖行缺失）**：v7 新增"任务分层 ↔ Phase 映射表"。**形式闭环但有歧义**（当前 v7 P1-3 指出核心任务六归口歧义）。
- **v6 P2-4（路线图层面 OOD 边界渗入）**：v7 形式上做了"约定段落"封口，但**实质未做 30+ 处命名的下沉**。**形式闭环实质未闭环**（当前 v7 P1-1 是本项的持续）。
- **v6 P2-5（任务分层映射缺失）**：v7 新增任务分层映射表解决。✓

历史持续存在的问题：
- **v3 P2-9（核心任务六 Phase 归口歧义）**：v4 修订过（Phase 3.6 准出描述收紧），v7 又以"两个阶段写在一行"的方式反向回潮（当前 P1-3）。需在 v8 彻底解决。
- **v1 P2-16（信息重复+轻微不一致）变体**：v7 P2-1 阅读指引与映射表信息重复，是历史 v1 P2-16 的变体回潮。
- **v4 修订说明与 iteration_history 计数不一致**：v7 P3-1 / N2 是历史修订说明对账问题的延续。

历史已闭环、当前 v7 已确认闭环的不再重复列出。

## 上一轮产出路径

C:\Develop\Software\AIMedicalSys\Harness\redeliberations\202606150151_项目实现路线图设计\a_v7_tech_v1.md

## 用户需求

C:\Develop\Software\AIMedicalSys\Harness\redeliberations\202606150151_项目实现路线图设计\requirement.md
