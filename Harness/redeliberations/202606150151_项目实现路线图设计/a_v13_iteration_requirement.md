根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### 严重问题 1：P0-1.2 字节数事实错误（v10 同类错误重复）

- **问题**：v12 修订说明 L805 原文"v12 文档体量：实测 110,858 字节（较 v11 减少 427 字节）"与实测 114,522 字节不符（差 3,664 字节）；v12 实际为 +3,237 字节增长（v11 = 111,285 字节 → v12 = 114,522 字节）。"实测"措辞等同以"已核测"形式声明错误事实。
- **建议方向**：v13 修订说明字节数必须用 PowerShell `Get-Item -LiteralPath "a_v13_tech_v1.md" | Select-Object Length` 在修订说明撰写完成后即时核测；若追加内容后字节数变化，须回滚至说明对应字节数或在说明中追加"说明写完后追加 X 字节（追加内容：……）"明确交代。

### 严重问题 2：P0-1.5 元信息下沉未执行，v12 实际体量反向增长

- **问题**：v12 实际字节数 114,522 超出目标 ≤ 95,000 字节达 19,522 字节（+20.5%）；v12 实际字节数较 v11（111,285 字节）反向增长 +3,237 字节（+2.9%）。v12 仍保留 ~170 行元信息（附录 A.1~A.3 / 附录 B / 附录 D / 修订说明 v6/v7/v8/v10/v11）。
- **建议方向**：v13 必须执行元信息下沉清单（~178 行 / ~23,000 字节）：
  - 附录 A.1~A.3（v1~v4 历次修订摘要）下沉至 `iteration_history.md`，文档仅保留指针"历次修订见 iteration_history.md"
  - 修订说明 v6 / v7 / v8 / v10 / v11 / v12 同样下沉至 `iteration_history.md`，仅保留修订说明（v13）作为本轮修订记录
  - 附录 B（已关闭风险）下沉至 `risk_register_closed.md`，仅保留指针
  - 目标：v13 ≤ 95,000 字节 / ≤ 750 行

### 一般问题 1：P1-3 退药申请状态机 6 态未在正文显式枚举

- **问题**：v12 仅在 Phase 0.6 L92 Tier 4 状态机契约清单追加了"退药申请状态机六态"（计数字），但未在 Phase 4.2 / 4.3 后追加 6 态显式枚举（与 L299 调拨单 6 态显式枚举的格式不一致）。
- **建议方向**：v13 在 Phase 4.2 药房域行后追加"**退药申请状态机 = 草稿 / 审批中 / 已通过 / 已执行（库存回补+缴费记录联动变更）/ 已拒绝 / 已撤销**"显式枚举；Phase 0.6 L92 Tier 4 状态机契约清单同步列出 6 态字面量。

### 一般问题 2：P1-4 病历状态机未在 Phase 0.6 Tier 4 追加 + 处方状态机未扩展为 6 态

- **问题**：v12 Phase 0.6 L92 Tier 4 状态机契约清单仍只有 5 个状态机，无"病历状态机"行；Phase 4.2 L298 药房域处方状态机仍为 4 态，未扩展为 6 态。
- **建议方向**：v13 在 Phase 0.6 L92 Tier 4 状态机契约清单追加"**病历状态机 = 病历生成中 / 医生确认中 / 已保存 / 完整版 / 已修订**"；Phase 4.2 L298 药房域行将处方状态机从 4 态扩展为 6 态（**AI 审核中 / 医生确认中 / 已提交 / 已发药（待取）/ 已发药（已取）/ 已退药**，与现有 4 态融合）。

### 轻微问题：P2 协作硬约束表 L539 历史叙述残留

- **问题**：v12 L539 仍保留历史叙述"**Phase 3 已将原'包 D（核心 2 + 核心 3 合并）'按可分配性硬约束拆分为 D-AI1 / D-AI2**"——违反归一化要求。
- **建议方向**：v13 L539 改为"**每个 Phase 子任务包对应单一职责或单一 AI 能力；D-AI1（AI 处方审核 3.4.2）/ D-AI2（AI 病历生成 3.4.3）以与 Phase 4.5 的 D1/D2 解耦命名**"——删除"Phase 3 已将原'包 D'按可分配性硬约束拆分"历史叙述。

## 历史迭代回顾

- **已解决的问题**：
  - P0-1.3 / P0-1.4 双权威索引闭环问题在 v12 已完成（13 项 AI 降级路径、AI 进阶底座职责分工统一指向附录 C / Phase 5.3 末段对照表），v12 修订说明 L796-L798 已正确落地。
  - P0-2 Tier 2 颗粒度细化问题在 v11 已正确执行（"模块级端点冻结时机 = 该模块首个消费方所在阶段启动前；多消费方模块的端点清单分阶段冻结"），v12 已保留。

- **持续存在的问题**：
  - **P0-1.5 元信息下沉** — 已连续 4 轮未实质执行（v9 P1-2 → v10 P0-1.5 → v11 P0-1.5 → v12 仍未修复），元信息从 v11 的 ~158 行增至 v12 的 ~170 行，每次修订说明新增都未触发元信息下沉。**v13 必须重点解决，否则文档体量目标完全无法达成**。
  - **P0-1.2 字节数事实错误** — v10 首次出现（差 3,723 字节），v12 再次出现（差 3,664 字节）；表明字节数核测流程仍未嵌入修订说明工作流，"实测"措辞仍让错误事实看似已核测。**v13 必须将字节数核测嵌入修订说明撰写工作流**。
  - **P1-3 退药申请状态机显式枚举** — 已连续 2 轮采用"修订说明替代正文"工作路径（v11 修订说明 L785、v12 修订说明 L800），正文 Phase 4.2 / 4.3 始终未实际落地。**v13 必须将状态机从修订说明迁移到正文 Phase 0.6 Tier 4 + Phase 4.2/4.3 显式枚举**。
  - **P1-4 病历/处方状态机扩展** — 与 P1-3 同模式，已连续 2 轮仅在修订说明承担状态枚举定义职责，正文 Phase 0.6 Tier 4 + Phase 4.2 始终未实际落地。**v13 必须将状态机定义从修订说明迁移到正文**。

- **新发现的问题**：
  - **P2 协作硬约束表 L539 历史叙述残留** — v12 首次被审查识别。v11 修订说明 L787 自检漏判"经核查该行无历史标签残留"，但实测该行仍含历史叙述。**v13 必须执行归一化，移除 v3~v12 历史叙述**。

## 上一轮产出路径

C:\Develop\Software\AIMedicalSys\Harness\redeliberations\202606150151_项目实现路线图设计\a_v12_tech_v1.md

## 用户需求

C:\Develop\Software\AIMedicalSys\Harness\redeliberations\202606150151_项目实现路线图设计\requirement.md