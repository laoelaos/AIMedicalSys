# 再审议判定报告（v10）

## 判定结果

RETRY

## 判定理由

组件 B 诊断报告（b_v10_diag_v2.md）经质询报告（b_v10_challenge_v2.md）LOCATED 确认，所识别的质量问题**证据充分、逻辑自洽**。但报告中**包含 2 项严重等级（P0）问题 + 2 项一般等级（P1）问题 + 1 项轻微等级（P2）问题**，依据判定标准"审查报告包含严重或一般等级的问题 → RETRY"，需重新运行组件 A。

**质询循环状态**：内部循环最大 12 轮 / 实际 2 轮（v1→v2 一次质询即 LOCATED），非耗尽式终止，而是**提前终止于 LOCATED**——说明质量问题已被准确定位，组件 A 可据此改进。

**严重等级问题**：

1. **P0-1.2：v10 修订说明（v10）L754 字节数事实错误**（严重）—— 实测 106,136 字节 ≠ 自述 102,413 字节（差 3,723 字节）；v10 实际为 +3,078 字节增长而非 -645 字节缩减。**直接动摇 v10 修订说明（v10）作为本轮修订轨迹凭证的可信度**；首次阅读者若用 `wc -c` 复核会发现与文档自述不符。

2. **P0-2：模块级接口契约冻结时机（Phase 0.6 Tier 2）颗粒度过粗**（严重）—— Tier 2 措辞"Phase 1 启动前冻结"为 7 模块一刀切式要求，但 7 模块首个消费方启动阶段分布 Phase 1~5（patient=Phase 2 / doctor=Phase 3 / exam-lab-pharmacy=Phase 4 / admin=Phase 1+5 / ai-ability=Phase 2~5 / device-gateway=Phase 4），强制远期消费方端点 Phase 1 启动前冻结与"分层递进"约束存在内在张力。**直接影响骨架文档对"多人并行贡献"硬约束的可执行性指引**。

**一般等级问题**：

3. **P1-3：退药申请状态机无显式状态枚举**（一般）—— 5 大状态机（缴费 5 态 / 调拨 6 态 / 盘库 4 态 / 处方 4 态 / 挂号 4 态）均有显式状态枚举；仅退药申请状态机在 Phase 4.3 通过"被拒兜底"间接提及"已拒绝"态，其余状态（草稿/审批中/已通过/已执行/已撤销）未固化。属 P1 状态机定义完整性问题，首次 OOD 阶段需先在路线图层固化状态枚举才能开工。

4. **P1-4：病历/处方"草稿 vs 完整版"边界未显式定义**（一般）—— Phase 4.2 L290"病历——Phase 3 起即可见草稿，本阶段接入完整版"分界点仅靠时序隐式区分；处方状态机仅定义"待发药/已发药（待取）/已发药（已取）/已退药"4 态（侧重发药流转），未定义"草稿 vs 完整版"维度。患者端 3.1.5 可见性时序依赖此定义。

**轻微等级问题**（不影响判定）：

5. **P2：正文中残留"v8 收口" / "v6 重命名"等历史标签**（轻微）—— Phase 0.6 L93 含"v8 合并"历史标签、协作硬约束表 L526 含"v6 重命名"历史标签，违反 v9 P1-1 / v8 P1-3 归一化要求。

**v9 历史问题回归**：8 项中 5 项完全修复（v9 P0-1 / P1-1 / P1-2 / P1-4 / P1-5）、2 项部分修复（v9 P1-3 / v9 P1-6）、1 项未修复且新增事实错误（v9 P0-2 → P0-1.2）。v10 已基本实现"项目实现路线图"定位，但上述 4 项问题需在 v11 集中解决。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：v10 修订说明（v10）L754 字节数事实错误——实测 106,136 字节 ≠ 自述 102,413 字节（差 3,723 字节）；v10 实际为 +3,078 字节增长而非 -645 字节缩减
- **所在位置**：a_v10_tech_v1.md 修订说明（v10）L754
- **严重程度**：严重
- **改进建议**：(a) 用 `Get-Item -LiteralPath "a_v10_tech_v1.md" | Select-Object Length` 或 `wc -c` 重新核测 v9 与 v10 的实际字节数；(b) 修订说明（v10）L754 同步更新为正确数值；(c) 若文件在说明写完后被追加内容，应回滚至说明对应的字节数，或在说明中追加"说明写完后追加 X 字节（追加内容：……）"明确交代

- **问题描述**：模块级接口契约冻结时机（Phase 0.6 Tier 2）颗粒度过粗——Tier 2 一刀切式要求 7 模块端点 Phase 1 启动前冻结，与"分层递进"约束存在张力
- **所在位置**：a_v10_tech_v1.md Phase 0.6 L92
- **严重程度**：严重
- **改进建议**：(a) 将 Tier 2 措辞修订为"**模块级端点冻结时机 = 该模块首个消费方所在阶段启动前；多消费方模块的端点清单分阶段冻结**"；(b) 同步在 Phase 0.5 任务分配段 A~G 后增加"**各模块端点冻结时点表**"——A=Phase 1 启动前 / B=Phase 2 启动前 / C=Phase 3 启动前 / D=Phase 1 启动前 / E=Phase 1 启动前（3.4.1）/ Phase 2 启动前（3.4.2/3/10）/ Phase 3 启动前（3.4.4/5/6/7/9/11）/ F=无业务端点 / G=Phase 3 启动前（软预备条目不要求冻结，仅占位）

- **问题描述**：退药申请状态机无显式状态枚举——5 大状态机中仅退药申请状态机未固化状态枚举
- **所在位置**：a_v10_tech_v1.md Phase 4.2 药房域行 / 4.3 退药角色矩阵段
- **严重程度**：一般
- **改进建议**：(a) 在 Phase 4.2 药房域行 / 4.3 退药角色矩阵段后追加显式状态枚举（参考调拨单 6 态格式）：**退药申请状态机 = 草稿 / 审批中 / 已通过 / 已执行（库存回补+缴费记录联动变更）/ 已拒绝 / 已撤销**；(b) 同步在 Phase 0.6 Tier 4 状态机契约清单中追加"退药申请状态机"行

- **问题描述**：病历/处方"草稿 vs 完整版"边界未显式定义——3.1.5 患者端可见性时序依赖此定义
- **所在位置**：a_v10_tech_v1.md Phase 4.2 L290
- **严重程度**：一般
- **改进建议**：(a) 在 Phase 0.6 Tier 4 状态机契约清单中追加"病历状态机"行——建议为：**病历生成中（AI 输出未确认） / 医生确认中（已编辑未保存） / 已保存（医生保存，患者端可见"草稿"） / 完整版（Phase 4 接入检查/检验数据后状态） / 已修订（医生修订后版本）**；(b) 处方状态机追加"AI 审核中 / 医生确认中 / 已提交（已记账） / 已发药（待取） / 已发药（已取） / 已退药"扩展（与现有 4 态融合）

- **问题描述**：正文中残留"v8 收口" / "v6 重命名"等历史标签——违反 v9 P1-1 / v8 P1-3 归一化要求
- **所在位置**：a_v10_tech_v1.md Phase 0.6 L93、协作硬约束表 L526
- **严重程度**：轻微
- **改进建议**：(a) Phase 0.6 L93 改为：**软预备条目总览**——删除"（原独立小节 0.7 已合并入 0.6）"括注；(b) 协作硬约束表 L526 改为：**每个阶段可拆解为可分配给不同开发者的子任务包** | 每个 Phase 子任务包对应单一职责或单一 AI 能力；D-AI1（AI 处方审核 3.4.2）/ D-AI2（AI 病历生成 3.4.3）以与 Phase 4.5 的 D1/D2（药库 / 调拨）解耦命名 | 全部——删除"Phase 3 已将原'包 D'按可分配性硬约束拆分"历史叙述
