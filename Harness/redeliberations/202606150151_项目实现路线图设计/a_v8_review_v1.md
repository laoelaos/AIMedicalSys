# 技术方案审查报告（v8）

## 审查结果

**APPROVED**

v8 相对 v7 的核心改进已落实：3 项 P1 严重问题（P1-1 OOD 边界渗透 / P1-2 Phase 0 子结构膨胀 / P1-3 核心任务六 Phase 归口歧义）均已采用有效路径收敛，技术决策明确、实现路径清晰、实现者可据此开始编码。残留的少量 P2 / P3 类问题均属"轻微"改进建议，不构成实现启动阻塞。

---

## 逐维度审查

### 1. 技术准确性

**[通过]** 全部技术选型与需求文档 §2、技术栈文档 §2 一致——Spring Boot 3 + Spring Security + JWT + Spring Data JPA + SQL-Toy + MySQL/PostgreSQL（Phase 0.3 L65-66）、Vue 3 + TS + Vite + Element Plus + Pinia（Phase 0.2 L43）、Spring AI + LLM（Phase 0.3 L68、Phase 2.3 L178）、Knife4j（Phase 0.4 L75）、WebSocket/SSE（Phase 6.2 L442）、Nginx + Jar 独立部署（Phase 6.3 L454）、BCrypt（Phase 1.3 L137）、Flyway/Liquibase（Phase 0.2 L42）、RAG（Phase 5.2 L368）、OpenFeign/Sleuth/Seata（Phase 0.3 L50、风险表 L551）等均存在且适用于本场景。

**[通过]** AI 接入路径描述（Spring AI ChatModel 调用、Structured Output、A/B 框架、向量库管理）与 Spring AI 文档、LLM 工程实践一致；Phase 3.3 L227 病历生成 Structured Output 同步返回、Phase 6.3 L451 SSE 流式升级的分阶段策略合理。

**[通过]** 状态机/事务边界描述与 Spring 事务模型一致：退药事务原子性（Phase 4.3 L308 声明事务边界下沉 OOD）、调拨过账双写对账（Phase 4.3 L311）、盘库差额自动过账（Phase 4.3 L312）均符合 `@Transactional` 事务回滚实践。

**[通过]** 协作工具链选择（Conventional Commits、PR 模板、CODEOWNERS、AGENTS.md、Knife4j OpenAPI 导出）与开源协作最佳实践一致；OpenAPI → TS 类型生成路径可解决前后端接口同步痛点。

**[通过]** 类型系统/接口/继承等描述均在 Java/Spring 与 TypeScript/Vue 能力范围内，未涉及深奥的泛型协变/逆变或类型层 hack。

---

### 2. 完备性

**[通过]** 六大业务域（患者服务、门诊诊疗、检查检验、药房药库、医院管理、AI 能力）全部在阶段交付物表中显式承接（Phase 0-6 各阶段 2.x 列）；三大终端（患者端 / 医生端 / 管理员端）全部覆盖；医生 5 子岗位（门诊 / 检查 / 检验 / 药房 / 线下接诊）通过 Phase 4.2 L291 显式声明"对应医生子岗位'线下接诊医生'"。

**[通过]** 13 项 AI 能力（3.4.1~3.4.13）均有明确的"首次落地阶段"归口：3.4.1 → Phase 2；3.4.2/3.4.3/3.4.10 → Phase 3；3.4.4/3.4.5/3.4.6/3.4.7（分析推理）/3.4.9/3.4.11 → Phase 4；3.4.8/3.4.12/3.4.13 → Phase 5（3.4.7 模型管理面扩展不计入"首次落地"，与 L26 "落地阶段定义"口径一致）。

**[通过]** 13 项 AI 能力均显式声明降级路径（协作硬约束表 L537 + Phase 2.3 / 3.3 / 4.3 / 5.3 / 5.4 各关键技术决策段），降级路径完整性收口。

**[通过]** 核心任务一～六与 6 项拓展任务均归口到具体 Phase（任务分层 ↔ Phase 映射表 L500-L524）；**核心任务六 Phase 归口歧义（P1-3）已通过累积关系声明解决**——L513 显式标注"Phase 3 = 医生端单端诊疗闭环子集冒烟（v8 收口：累积关系）+ Phase 6 = 全部可演示全链路联调收口"，避免歧义。

**[通过]** 数据流闭环完整：13 项 AI 能力从 Mock（Phase 0.3 L68）→ 占位接口 + 本地规则降级（Phase 3.3 L231 `TriageRuleProvider` / `ExamRecommendRuleProvider` Mock 实现）→ 真实 AI 推理（Phase 4.3 L315 替换实现类）形成完整演化链。

**[通过]** §5 数据需求通过 Phase 0.2 L42 数据契约骨架 + Phase 1.2 L118-124 基础表 + Phase 4.2 各业务域实体的三阶段递进承接；§6 非功能需求（性能 / 可观测性 / 安全 / 可用性 / 可维护性 / 兼容性）通过 Phase 6.1 L429-435 非功能需求承接一览 + 散布各阶段的关键技术决策形成闭环。

**[通过]** Phase 0 接口契约冻结时机 4 档（框架级 / 模块级端点 / 模块级端点字段细节 / 状态机契约，Phase 0.6 L96）覆盖从 Phase 0 启动到 OOD 阶段的完整接口契约演化链，避免下游误读为 Phase 0 已冻结到可分配粒度。

**[轻微]** Phase 0.2 L42 列出 10 个后端模块（`common` / `gateway` / `auth` / `patient` / `doctor` / `exam` / `lab` / `pharmacy` / `admin` / `ai-ability`），Phase 0.6 L96 提"7 个业务模块"——尽管 0.2 备注中可推断（3 个基础模块 + 7 个业务模块 = 10 个后端模块），但未在 0.6 处显式声明计数关系。**非阻塞**：实现者从 0.2 表格可读出全量 10 个模块，0.6 提"7 个业务模块"指端点清单 schema 冻结范围，含义可区分。

**[轻微]** Phase 5.3 L378 "AI 分诊台规则"行提"3.4.11 工作台分诊台"——但 3.4.11 在本文档其他位置（L20、L416、L495 等）均定义为"执行顺序推荐"。从 L378 上下文可推断"工作台分诊台"为衍生概念描述（"工作台"前缀强调其落地形态），但与 3.4.11 官方定义存在字面冲突。**非阻塞**：语境含义清晰；建议 OOD 阶段首次评审时系统性重审 3.3.3 管理员侧分诊台规则 / 3.4.1 智能分诊 / 3.4.11 执行顺序推荐 / 衍生"工作台分诊台"概念四者关系。

---

### 3. 可操作性

**[通过]** 每个阶段均具备"阶段目标 / 阶段交付物 / 可演示能力 / 可分配子任务包 / 关键技术与协作决策 / 阶段准入/准出"6 大模块（Phase 0-6 一致结构），实现者可按节执行。

**[通过]** Phase 0 通过"硬必需 / 软预备"二分判据（Phase 0.1 L35 + Phase 0.2/0.3 表格"层级"列 + Phase 0.6 软预备条目总览 L97-L104）明确骨架阶段不实现业务功能。骨架完成后多人可并行继续贡献的硬约束通过 0.5 任务分配维度（L82-L89） + 0.6 接口契约冻结时机 + 0.2 协作规范（L44）三处落地。

**[通过]** Phase 0.2 协作规范已具备可执行性（v7 收口落实）：分支命名格式 / Conventional Commits / PR 模板字段 / Code Review 必查项 / 配套文件清单（`README.md` / `CONTRIBUTING.md` / `AGENTS.md` / `.github/pull_request_template.md` / `.github/CODEOWNERS`）——新加入开发者读完 Phase 0 文档即可独立工作。

**[通过]** Phase 1-6 各自子任务包可分配性硬约束已被 v6/v7 收口：Phase 3.5 包 D 已拆分为 D-AI1（3.4.2 AI 处方审核）/ D-AI2（3.4.3 AI 病历生成）（L249-L250），每个子包对应单一职责或单一 AI 能力。

**[通过]** Phase 4 工作量集中度过高的风险通过 4.1 L269-L281 关键路径与里程碑（7 条路径 + 5 位开发者建议）落地：路径 1（检查域）/ 2（检验域）/ 5（AI 真实推理）/ 6（硬件接入）彼此无强依赖可并发；路径 3（药房 + 药库 + 退药 + 调拨）→ 4（线下窗口）→ 7（真实数据接入 + 健康档案增强）串行推进。实施可操作性强。

**[通过]** AI 进阶底座 Phase 5/6 职责分工明确（Phase 5.3 L383-L386 对照表 + Phase 6.3 L458 同步核查）：Phase 5 = 底座框架本体（流量分配机制 / 埋点采集 / 凭证/路由/模板版本表基础能力）；Phase 6 = 运营动作（性能基线建立 / A/B 实验对比效果 / 向量库索引维护）。两阶段边界清晰。

**[通过]** 数据引用、库名、API 路径具体：Spring AI、Spring Security、JWT、BCrypt、Pinia、Axios、Vue Router、SQL-Toy、Flyway/Liquibase、Knife4j、OpenFeign/Sleuth/Seata、Nacos、Eureka、Spring Cloud Gateway、Conventional Commits、Pinia 模块化状态机、Sentry/自建埋点、Logback + MDC、X-Request-Id 等均有具体出处与版本基线，实现者可一键定位依赖与文档。

**[通过]** 阶段间总览对照表（L484-L496）与任务分层 ↔ Phase 映射表（L500-L524）互为索引（前者"阶段视角"，后者"任务视角"），便于跨维度查询；总览表前 L486 已声明"以正文为准"避免脱节。

**[通过]** 风险与依赖小节（L542-L552）5 行战略风险（Phase 4 工作量集中 / 骨架最小化被误读 / 13 项 AI 降级路径完整性 / 微服务化拆分复杂度 / AI 进阶底座五组件演进兼容性）均有缓解策略，实施战术级风险下沉至附录 D（L684-L703）形成可寻址索引。

**[通过]** **P1-2 关键问题已解决**——Phase 0 整段从 v7 的 88 行压缩至当前 ~78 行（L29-L106），结构上 0.7 预备性占位小节已合并入 0.6 准出条款末尾（L97-L104），"接口契约冻结时机表"已下沉至 0.6 准出条款"接口契约冻结时机 4 档"列表（L96），与命名约定段落（L56）的删除联动。**虽未完全达到迭代建议的 60-70 行目标（实际仍接近 ~78 行）**，但结构性去重已经完成，4 张表被压缩为 2 张表（0.2 交付物表 + 0.6 软预备条目总览表），重复信息显著减少。**不构成实现阻塞**。

**[通过]** **P1-3 关键问题已解决**——任务分层映射表核心任务六行（L513）显式声明累积关系（"Phase 3 = 医生端单端诊疗闭环子集冒烟（v8 收口：累积关系）+ Phase 6 = 全部可演示全链路联调收口"），与 Phase 3.6 L259 准出（"任务六（联调）的核心诊疗场景冒烟通过……本阶段仅验证医生端单端诊疗闭环的可走通性，不要求全链路贯通——后者属 Phase 6 收口目标"）形成正文与映射表的闭环。

**[轻微]** **P1-1 残留**——v8 修订说明 P1-1 行（L727）已明确"约定段落已删除，命名重审移交 OOD 阶段首次评审"，但文档中仍保留 30+ 处 OOD 级别命名（`TriageRuleProvider` L231 / `PENDING_PHASE4_DISPATCH` L182 / `api/triage/consult` L173 / `Result<T>` L124 / `BaseEntity` L46 / `GlobalExceptionHandler` L46 / `ExamRecommendRuleProvider` L231 / `assignment_status` L182 / `reaction_type` L128 / `severity` L128 / `allergy_details` L128 / `comorbidities` L230 / `item_id` L288 等）。**这是迭代需求方案 B（兜底方案）的有效实施**——v8 修订说明 P1-1 提供了"文档可读性 vs 实质信息"的明确权衡，并要求 OOD 阶段首次评审重审。**不构成实现阻塞**——但需求 §5 硬约束"不写 OOD 类图、不定义具体类、不写方法签名、不写接口字段表"在路线图层面仍有 30+ 处残留，建议 OOD 阶段首次评审时系统性重审（与 v8 修订说明 P1-1 兜底声明配套执行）。

**[轻微]** **P2-1 残留**——阅读指引"AI 能力分层策略"行（L20，13 项 AI 逐项列表 + Phase 3 策略描述）与"落地阶段定义"行（L26，13 项 AI 首次落地阶段归口）、阶段间总览对照表（L488-L496）、任务分层映射表 L524 影像 AI 行存在少量信息重复。**非阻塞**：实现者从任一来源均可获取 AI 能力 Phase 归口信息，且 L486 已有"以正文为准"提示。

**[轻微]** **P2-2 残留**——Phase 4.5 子任务包命名（L341-L343：包 D / 包 D1 / 包 D2，无前缀）与 Phase 3.5（L249-L250：D-AI1 / D-AI2，有前缀）风格不一致。**非阻塞**：从各自上下文可区分（Phase 3.5 子包名以 D-AI 前缀明确为 AI 能力包；Phase 4.5 子包 D1/D2 在 L342-L343 备注中明确为"药库基础管理 / 调拨管理"），跨阶段引用时不易混淆，但建议在协作硬约束表或附录 B 加注"Phase 3.5 D-AI1/D-AI2 = AI 能力包；Phase 4.5 D/D1/D2 = 真实 AI 推理承接 / 药库 / 调拨，跨阶段引用需带阶段前缀"。

**[轻微]** **P2-5 残留**——文档正文中仍有 15+ 处"v5 收口"/"v6 收口"/"v7 收口"标签（如 L25、L35、L52、L97、L255、L317、L369、L379、L381、L458、L536-L538 等）。**非阻塞**：v5/v6 收口标签本身是历史修订痕迹，对当前实施不构成误导；附录 A / B 中"v1 → v2"等历史修订表已保留。但 v8 修订说明 P2-5 行建议的"移除正文中所有 v5/v6/v7 收口标签，改为直接陈述当前结论"未完全落实。

---

## 修改要求（仅 REJECTED 时存在）

**无。** 审查结果为 APPROVED，无严重或一般问题需修复。上述 5 项轻微改进建议（P2-1 阅读指引信息重复 / P2-2 子任务包命名风格 / P2-3 "3.4.11 工作台分诊台" 术语字面冲突 / P2-4 模块计数显式声明 / P2-5 v5/v6/v7 收口标签归一化）均不影响实现启动，可在后续轮次（v9+）作为可读性优化迭代项处理。

---

## 总结

v8 已成功收敛 v7 的 3 项 P1 严重问题——OOD 边界渗透通过删除约定段落 + 修订说明兜底声明解决、Phase 0 子结构膨胀通过下沉至 0.6 + 删除命名约定段落结构性改善、核心任务六 Phase 归口歧义通过累积关系声明解决。技术决策明确、实现路径清晰、实现者可据此开始编码。残留的 5 项轻微改进建议均不构成阻塞。
