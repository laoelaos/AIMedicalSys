# 诊断质询报告（v1）

## 质询结果

LOCATED

## 逐维度审查

### 1. 证据充分性

**[通过]** §1.1 定义矛盾的 L293/L306-L307/L759/L785 引用均经核实，与 `Docs/04_ood_phase0.md` 实际内容一致。

**[通过]** §1.2 JDBC scope 矛盾的 L1407-L1411（H2 runtime）与 L1413（MySQL/PostgreSQL compile）引用准确。

**[通过]** §2.1 错误码命名约定缺口的需求 §3.4 (L814-L818) 引用准确，`_AI_` 命名约定和 5 处修订点与 `Docs/01_requirement.md` 一致。

**[通过]** §2.2 权限矩阵缺口的角色映射与 DataPermissionEvaluator 两点缺口均有明确文献引用，降级依据段落完整覆盖两子点。

**[通过]** §2.3 DTO 字段约束缺口的 chiefComplaint 5–500 字符 (L825)、dialogueText 50–10000 字符 (L861) 等均与需求文档一致。

**[通过]** §3.2 SecurityConfig 超前实现的分析引用 L768-L775、L780-L784 准确。

**[通过]** §4.1 CSRF 未禁用的代码引用 L768-L771 准确。

**[通过]** §4.2 @SQLRestriction 局限性的引用 L513 准确。

**[问题-轻微]** §1.1 中报告断定 security 依赖的"实际原因"为 "`GlobalExceptionHandler` 中的 `@ExceptionHandler` 方法需要捕获 `AuthenticationException` 和 `AccessDeniedException` 类型"，但 OOD §4.5 (L784) 显示这些异常由 SecurityConfig 的 `AuthenticationEntryPoint`/`AccessDeniedHandler` 处理（非 GlobalExceptionHandler），GlobalExceptionHandler 的职责描述 (L492-501) 也未明确列出这两类异常。该断定缺乏 OOD 文档的直接支撑，属于推测而非验证。不过该不精确性不影响核心诊断结论（L293 的"用于 SecurityConfig"表述与 SecurityConfig 实际归属的矛盾）。

### 2. 逻辑完整性

**[通过]** 从问题现象到根因的因果链完整。如 §2.1 从"需求要求 _AI_ 命名约定"到"OOD §8.2 未要求 MockAiService 返回值遵循该约定"到"Phase 2+ 开发者可能返回不符合约定的错误码"，形成完整的"需求定义 → OOD 映射缺口 → Phase 1+ 衔接风险"链。

**[通过]** §2.2 对原归类于"偏离需求文档"的子点（线下接诊医生、药房医生跨模块授权）已正确移入 §8.3（跨阶段衔接建议），消除分类矛盾。

**[通过]** 优先级判定统一标准已建立（P0=阻塞验收标准/P1=影响衔接质量/P2=改善性），全文保持一致。§1.1 已从 P1 降级为 P2，解决优先级倒挂问题。

**[通过]** §8.1 修复指令已消除"OOD 应补充"与"不属于 OOD 修改范围"的表述矛盾，统一为"建议另建独立文档"。

**[通过]** 影响范围判定合理：PasswordEncoder 隐藏风险标记为 P1（隐蔽性高但 Phase 0 排查无额外成本），@SQLRestriction 局限性明确说明 Phase 0 无实际影响，CSRF 远期风险通过脚注标注。

### 3. 覆盖完备性

**[通过]** 覆盖所有 5 个诊断维度：定义矛盾 (§1)、事实错误 (§6 声明未发现)、逻辑错误 (§6 声明未发现)、偏离需求文档 (§2)、偏离路线图 Phase 0 (§7)。

**[通过]** 额外覆盖技术风险提示 (§4)、跨阶段衔接建议 (§8)，且通过维度和边界声明明确标注这些内容不属于核心诊断维度。

**[通过]** 所有 7 项审查意见均已回应（对应 v9 修订说明），包括 §2.2 降级依据补充、§8.2 简化、§2.1"或"模式修复、§8.3 可操作性提升、§8.1 矛盾消除、PasswordEncoder/CSRF 副作用评估补充、§1.1 优先级降级。

**[通过]** 报告解决了持续存在的三方面问题：修复建议超出边界（§8.2 已简化）、"或"连接歧义模式（§2.1 已修复）、可操作性不足（§8.3 已补充具体行动项）。

**[通过]** 各节修复者须知均有明确行动指向，无"仅建议参考"的无指向性描述。

## 质询要点

无。所有发现均属轻微等级，不影响诊断总体可信度。
