# 再审议判定报告（v5）

## 判定结果

RETRY

## 判定理由

组件B内部循环在第1轮（实际轮次1，最大轮次12）即 LOCATED，质询确认全部4个问题证据充分、逻辑完整、覆盖完备。但诊断报告包含1个严重问题（Issue 1测试与Issue 2修复方案逻辑矛盾）、1个中等问题（生产脏数据存在性缺乏关联说明）和2个一般问题（clear()使用不一致、UserRepository间接写入路径未确认），合计1严重+1中等+2一般，满足RETRY条件。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：Issue 1的测试代码与Issue 2的修复方案存在逻辑矛盾——`user_shouldAllowNullPassword()`测试允许password为NULL，而Issue 2要求添加NOT NULL约束；同时M2映射点描述将password无NOT NULL定性为"应为NULL可接受"，与Issue 2的缺陷定性互斥。
- **所在位置**：a_v5_diag_v1.md:21（M2映射点描述）、:70-83（测试方法）、:319（Issue 2修复指引）
- **严重程度**：严重
- **改进建议**：删除`user_shouldAllowNullPassword()`测试并替换为验证NOT NULL约束的测试；修正M2行描述；补充交叉影响备注。

- **问题描述**：生产脏数据存在性（password IS NULL的记录）与"无生产代码路径"的陈述并置，缺乏过渡说明，造成事实断层。
- **所在位置**：a_v5_diag_v1.md:325-333
- **严重程度**：一般（中等）
- **改进建议**：补充过渡说明，澄清脏数据可能来自数据导入/种子脚本、旧版本代码、或其他系统直接写入。

- **问题描述**：Issue 1测试示例在`entityManager.clear()`使用上不一致，7个测试中3个使用、4个未使用，与现有`EntityMappingIT.java`风格不统一。
- **所在位置**：a_v5_diag_v1.md:105、132、153（使用clear）；:56-67、70-83、172-185、218-240（未使用clear）
- **严重程度**：一般
- **改进建议**：统一约定——要么全部使用clear()验证完整DB读写路径，要么保持现有风格并说明原因。

- **问题描述**：Issue 2代码路径排查仅搜索`new User()`，未确认是否存在通过`UserRepository`间接写入或SQL脚本插入User数据的路径。
- **所在位置**：a_v5_diag_v1.md:325-329
- **严重程度**：一般
- **改进建议**：补充对`UserRepository`引用的全局搜索，确认无间接写入路径后明确说明。
