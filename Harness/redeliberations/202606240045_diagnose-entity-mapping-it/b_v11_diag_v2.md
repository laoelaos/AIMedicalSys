# 质量审查报告：a_v11_diag_v1.md

## 审查概况

审查轮次：第 11 轮
审查视角：需求响应充分度、整体深度与完整性、可操作性

## 需求响应充分度评估

### 需求 1：EntityMappingIT 缺少 User/Role/Post 集成测试

**响应状态：充分**

- 映射点表 M1-M15 完整覆盖三个实体的关键映射，代码行号精确至字段级别
- 提供 7 个完整测试方法示例（含方法签名、断言逻辑、异常处理），修复者可复制后直接使用
- 测试策略说明覆盖级联策略、关联表验证、unique/NOT NULL 约束验证策略
- 测试环境能力边界已显式声明（`sql.init.mode: never` 导致 `schema.sql` DDL 不可验证），并标注了对优先级排序的影响
- 交叉影响备注覆盖 Issue 2（password NOT NULL）和 Issue 4（enabled 默认值）的时序依赖

### 需求 2：password 无 NOT NULL 约束

**响应状态：充分**

- 根因精确至 `User.java:28` 及 `schema.sql:16`
- 代码路径排查覆盖 `new User()`（13 处，均不在生产代码）和 `UserRepository`（无生产注入/调用）两个维度
- 生产脏数据清理提供三种方案的适用场景和操作，包含预检 SQL
- 与 Issue 4 清理 SQL 的交叉数据冲突已分析并给出推荐执行顺序（先 Issue 4 清理 enabled，再 Issue 2 禁用无密码账号）
- 操作顺序建议完整（预检→业务决策→清理→DDL→验证）

### 需求 3：DDL 中 deleted 列 NOT NULL 约束与 BaseEntity 不一致

**响应状态：充分**

- 16 张受影响表逐张列出，含 schema.sql 行号
- 四步生产迁移方案（预检→清理→ALTER TABLE→验证）完整可执行
- 业务影响评估包含预审记录清单、分类处理策略（硬删除/恢复/备注审计）、执行窗口通知、备份与回滚方案
- @SQLRestriction 三值逻辑行为分析准确，并据此修正了 Issue 3 的优先级（v4 中从 P2 提升至 P1）

### 需求 4：enabled/visible 布尔字段跨实体缺少默认值

**响应状态：基本充分**

- 三项修复路径（A Java 默认值 / B @ColumnDefault / C 仅改 DDL）的权衡分析完整，并给出推荐方案（A+C 组合）
- 代码路径搜索覆盖 `getEnabled()` 调用（仅测例）、`enabled == null`（无匹配）、`.enabled` 引用（仅配置属性），确认无生产代码依赖 `enabled == null` 语义
- 已有 NULL 数据清理 SQL 已提供

**响应完整性不足**：见问题 1。

---

## 发现问题

### 问题 1：Issue 4 章节缺失与 Issue 2 清理策略的交叉引用

- **问题描述**：Issue 2 章节（第 423-433 行）详细分析了其清理 SQL（`SET enabled = 0 WHERE password IS NULL`）与 Issue 4 清理 SQL（`SET enabled = 1 WHERE enabled IS NULL`）之间的执行顺序依赖，明确指出先执行 Issue 4 再执行 Issue 2 的推荐顺序。但 Issue 4 自身的交叉影响备注（第 679 行）仅指向 Issue 1，未提及 Issue 2。如果执行者的工作范围涉及 Issue 4 而尚未阅读 Issue 2 的完整内容（例如按优先级顺序，Issue 4 在 P2 但执行者跳读或被分配单独修复 Issue 4），可能忽略此时序约束，导致清理 SQL 执行顺序颠倒，产生安全漏洞（无密码且无 enabled 值的记录被误置为启用）。
- **所在位置**：Issue 4 章节——交叉影响备注（第 679 行）、清理 SQL（第 697-706 行）
- **严重程度**：中等
- **改进建议**：在 Issue 4 的"交叉影响备注"中增加对 Issue 2 的引用，或直接在清理 SQL 前方以注释形式标注执行前提"此 SQL 应在 Issue 2 的方案 B（`SET enabled = 0 WHERE password IS NULL`）之前执行，参见 Issue 2 交叉数据冲突分析"。

### 问题 2：Issue 3 回滚方案覆盖范围不明确

- **问题描述**：Issue 3 的回滚方案（第 606-615 行）以 `sys_user` 表为例展示了备份创建（`CREATE TABLE sys_user_bak_YYYYMMDD AS SELECT * FROM sys_user`）和回滚 UPDATE 语句，其余 15 张表以"每张表逐一执行类似操作"注释带过。第 608 行仅说"每张表逐一执行类似操作"，第 612-614 行的回滚 SQL 写死了 `sys_user_bak_YYYYMMDD` 表名。从可操作性视角，该方案要求执行者为 16 张表逐一构建备份和回滚 SQL，但未提供完整模板，也无统一备份命名约定说明。实际执行中可能出现部分表遗漏备份、备份表命名不一致导致回滚脚本要逐张调整等风险。
- **所在位置**：Issue 3 生产迁移方案——回滚方案（第 606-615 行）
- **严重程度**：一般
- **改进建议**：仿照步骤 2 中 16 条 UPDATE 语句的完整罗列模式，提供 16 张表的备份和回滚语句模板，或明确标注统一命名约定（如 `{table_name}_bak_YYYYMMDD`）及操作要求"为全部 16 张表逐一执行备份及回滚操作，将上述模板中的 sys_user 替换为对应表名"。

---

## 整体质量评价

产出经 11 轮迭代审议，整体质量较高。四个方面需求的响应均达到或接近充分水平，事实准确性经过多轮交叉验证，修复指引可操作。上述两个问题均不涉及事实性错误，而是影响可操作完整性：问题 1 涉及跨 Issue 的时序依赖在文档内部的追踪完整性，问题 2 涉及迁移方案安全网的完备性。修复者可结合本报告补充交叉引用和回滚模板后进入执行阶段。

## 修订说明（v11）

| 质询意见 | 回应 |
|---------|------|
| 1. [严重] Issue 2（异常类型准确性）的质疑完全基于推测，审查者未在目标环境验证 | **接受**。本版已移除所有基于推测的质量问题。两个问题均基于产出文档自身的表述和结构分析，不依赖未经验证的环境假设。 |
| 2. [严重] 审查未按要求对四个方面需求的响应充分度进行系统性评估，也未从可操作性视角评估 | **接受**。本版新增「需求响应充分度评估」章节，从四个方面逐一评估产出对原始需求的响应程度，并指出不足。问题发现均从可操作性视角提出。 |
| 3. [一般] 审查结论"三个问题均不是阻碍执行的重大问题"与审查行为存在内在矛盾 | **接受**。本版已统一审查立场：两个问题均明确标注为对可操作性有实质影响的改进点，而非"不阻碍执行但仅供参考"的观察记录。 |
| 4. [一般] Issue 1（缺失@SQLDelete交互分析）被审查者自评"不影响修复方案正确性"但仍作为质量问题提出，依据不充分 | **接受**。本版未将此条纳入。当前报告中的质量问题均被认为对可操作性有实质影响。 |
