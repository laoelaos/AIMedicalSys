根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

组件B对 v2 诊断报告（`a_v2_diag_v1.md`，456 行）的质量审查定位到以下问题（质询报告 LOCATED 确认通过）：

### A. 事实错误（必须修正）
- **A1【高】3.4.9 `ExaminationRecommendResponse.disclaimerRequired` 误判为缺失**：OOD §8.2 行 1022-1025 实际已定义 `Boolean disclaimerRequired` 字段，诊断报告 §2.1 / §2.4 / §1 概述 / §1.1 覆盖率表中四处错误地将其归入"字段缺失"，属事实判断错误。

### B. 内部一致性问题
- **B1【中】§1.1 覆盖率 100% 声明与 §2.1 实际未为 3.4.13 列出独立字段偏差行不一致**：§1.1 覆盖率表将 3.4.13 标为"已覆盖（含 `PatientInfo` 偏差）"指向 §2.2 / §2.4，但二者均未真正核查 3.4.13 自身字段约束（`discussion_text` ≥ 50 字符、`summary` 项数 0-5 项且每项 ≤ 100 字符、`encounter_id` 必填及特殊校验规则）。
- **B2【中】§4 修复指引优先级标签与 §2 章节标题优先级标签不一致**：§4 第 2 / 3 项将 §2.3 / §2.4 标为【高优先级】，§4 第 7 项聚合 §2.5 / §2.8 / §2.9 为【中优先级】，但 §2.3 / §2.4 章节标题本身为【中优先级】，§2.8 / §2.9 标题为【低优先级】。
- **B3【低】五档优先级体系声明的"极低"档实际未在 §2 中出现**：§1 与 §4 均声明"高 / 中偏高 / 中 / 低 / 极低"五档，但 §2 全部 14 个子章节中无任何标注为【极低】，实际仅用 4 档。

### C. 关键遗漏
- **C1【中】§2.2 未纳入 OOD `PatientInfo.allergyDetails` 结构化字段**：OOD §8.2 行 941 实际定义 `List<AllergyDetail> allergyDetails`（含 `allergen` / `reactionType` / `severity` / `occurredAt` 四子字段，显式声明"缺省时回退到 allergies"），诊断报告 §2.2 仅讨论 `allergies: List<String>` vs `allergy_history: string` 偏差，未涉及结构化字段映射规则。
- **C2【中】OOD `PatientInfo.name` 字段超出需求未提及**：OOD §8.2 行 936 `PatientInfo` 包含 `String name`，但需求 §3.4.x 各能力的 `patient_info` 契约均未定义 `name`，诊断报告 §2.2 未提及"OOD 超出需求字段"维度。
- **C3【低】§4 第 1 项将 3.4.12 列为"Phase 0 必须对齐"缺乏路线图依据**：路线图 §0 将 3.4.1 / 3.4.2 / 3.4.3 列为"核心三件套"，未将 3.4.12（AI 医生排班）纳入 Phase 0 优先序列。

### D. 可操作性细节问题
- **D1【低】§2.3 "回收 vs 保留"清单中 JPA 关联映射边界不明确**：未明确关联表 schema（`user_role` 等）是否需在 Phase 0 迁移脚本中创建；`@ManyToMany` 等关联注解若不保留，User-Role 之间如何表示关联未说明。
- **D2【低】§2.10 第 3 项对 OOD 设计意图的解读偏差**：OOD §3.4 行 634 明确声明 FallbackAiService"作为装饰器**始终注册为 Bean**"，"始终注册"语义为无条件注册（无 `@ConditionalOnProperty`），诊断报告将其列为"未明示 `@ConditionalOnProperty`"属解读偏差，可能误导执行者误增条件注解。
- **D3【低】§2.1 修复连锁副作用第 3 项"前端 UI 分支"与 Phase 0 实际状态不符**：Phase 0 为"最小化骨架"，路线图 §0.2 验收仅要求"三端前端可一键启动到占位首页"，前端尚未基于具体 AI DTO 编写实际 UI，该副作用提示对 Phase 0 阶段不构成影响。

## 历史迭代回顾

分析历史反馈与当前反馈的关系：

### 已解决的问题（v1 反馈中提到，v2 已采纳，本轮不再提及）
- v1 反馈 1（3.4.3 整段遗漏）：v2 §2.1 已补 3.4.3 输入 4 项缺失 + 输出 8 字段结构化对照表，§2.4 第 4 项已补 3.4.3 偏差，§4 第 1 项已纳入 3.4.3 为 Phase 0 必须对齐。
- v1 反馈 2（3.4.11 整段遗漏）：v2 §2.1 已补 3.4.11 输入 5 项缺失 + 输出 4 项缺失 + P1/P2/P3 vs Integer 1-5 语义错位；§3 第 5 项已新增数值语义风格观察。
- v1 反馈 3（3.4.5 / 3.4.6 / 3.4.7 字段遗漏不完整）：v2 §2.1 三行均已补全必填字段与命名偏差。
- v1 反馈 4（3.4.10 `PrescriptionDraft` 字段语义偏差未关联标记）：v2 §2.1 已新增 3.4.10 行显式标注类型复用；§3 第 4 项已新增联动分析。
- v1 反馈 5（§2.6 / §2.7 优先级上调 / §2.8 / §2.9 下调 / §4 第 5 项 §2.7 单列）：v2 §1 概述、§2 各章节标题、§4 修复指引方向均已调整。
- v1 反馈 6（§3.3 JPA 越界判断复核）：v2 §2.3 已引入"路线图 §0.2 骨架必备 vs OOD 定义粒度对照表"；已补充"回收对象 vs 保留对象清单"。
- v1 反馈 7（§2.1 / §2.2 / §2.3 副作用提示缺失）：v2 §2.1 影响范围已补充 5 项连锁变更副作用；§2.3 已补充 3 项副作用；§4 第 1 项已区分"Phase 0 必须对齐的能力"与"Phase 1+ 启动前冻结的能力"。
- v1 反馈 8（§1.4 证据格式不一致）：v2 §2.1 已采用"需求 §X 行 Y / OOD §X 行 Z"双侧行号对照格式。
- v1 反馈 9（五档优先级体系未显式声明）：v2 §1 概述与 §4 修复指引方向开头已显式声明五档体系。
- v1 反馈 10（3.4.13 覆盖状态核查遗漏）：v2 §1.1 已新增"13 项 AI 能力诊断覆盖率核查"表；覆盖率修正值为 100%。
- v1 反馈 11（次要问题合并精简）：v2 §2.6 / §2.10 / §2.5 / §2.8 / §2.9 已按建议精确化或优先级调整。

### 持续存在的问题（v2 反馈与本轮 b_v2_diag_v1.md 均提及的重点项）
- **持续项 1**：3.4.9 `disclaimerRequired` 字段已被 OOD 定义却被误判为缺失——v2 反馈第 1 项（事实错误，高）与本轮 A1（事实错误，高）同源，本轮**必须修正**。
- **持续项 2**：3.4.13 自身字段约束未独立核查——v2 反馈第 2 项（中）与本轮 B1（中）同源，本轮**应修正**（建议选 A 选项：在 §2.1 新增 3.4.13 独立行）。
- **持续项 3**：§2.2 未提及 OOD `PatientInfo.allergyDetails` 结构化字段——v2 反馈第 4 项（中）与本轮 C1（中）同源，本轮**应修正**。
- **持续项 4**：§4 与 §2 章节标题优先级标签矛盾——v2 反馈第 3 项（中）与本轮 B2（中）同源，本轮**应修正**（建议以 §4 为权威回填 §2 章节标题）。
- **持续项 5**：OOD `PatientInfo.name` 字段超出需求未提及——v2 反馈第 5 项（中）与本轮 C2（中）同源，本轮**应修正**。

### 新发现的问题（本轮 b_v2_diag_v1.md 新识别）
- **新发现 1**：五档优先级体系的"极低"档实际未在 §2 中出现（B3，低）。
- **新发现 2**：§4 第 1 项将 3.4.12 列为"Phase 0 必须对齐"缺乏路线图依据（C3，低）。
- **新发现 3**：§2.3 JPA 关联映射边界不明确（D1，低）。
- **新发现 4**：§2.10 第 3 项对 FallbackAiService 设计意图解读偏差（D2，低）。
- **新发现 5**：§2.1 修复连锁副作用第 3 项"前端 UI 分支"与 Phase 0 实际状态不符（D3，低）。

### 修复策略建议
- **必须修正**：A1（事实错误，高）。
- **应修正**：B1 / B2 / C1 / C2（中等级别，影响覆盖率声明准确性与内部一致性）。
- **建议修正**：B3 / C3 / D1 / D2 / D3（低等级别，可作为可选精度提升）。

## 上一轮产出路径

`Harness/redeliberations/202606160104_ood_phase0_problem_diagnosis/a_v2_diag_v1.md`（456 行，未超 1000 行）

## 用户需求

`Harness/redeliberations/202606160104_ood_phase0_problem_diagnosis/requirement.md`
