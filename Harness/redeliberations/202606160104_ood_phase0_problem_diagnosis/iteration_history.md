## 迭代第 1 轮

1. **问题描述**：3.4.3 AI 病历生成 整段遗漏。诊断报告 §2.1 不符清单未涵盖 3.4.3，导致"核心三件套（3.4.1/3.4.2/3.4.3）"基线缺失。需补全输入契约 4 项缺失（`patient_id` / `encounter_id` / `stream` / 字符数约束）与输出契约 8 字段退化为单字符串的结构偏差。
   - 所在位置：`a_v1_diag_v1.md` §2.1 不符清单（行 33-53）；§5"本诊断范围内"声明（行 339-345）
   - 严重程度：严重
   - 改进建议：在 §2.1 新增 3.4.3 行，逐字段列出输入 4 项缺失 + 输出 8 字段退化的字段对照表；在 §2.4 补充 3.4.3 实际 OOD 定义与"对齐"承诺的偏差；在 §4 修复指引方向显式纳入 3.4.3，并指出"流式/非流式架构选型未在 OOD §8.2 中预留"是 Phase 2+ 流式输出的阻塞点。

2. **问题描述**：3.4.11 AI 执行顺序推荐 整段遗漏。诊断报告未涵盖 3.4.11（检查/检验医生工作台核心 AI 能力），且 `task_role` 必填字段缺失会导致岗位权限校验无法实施。
   - 所在位置：`a_v1_diag_v1.md` §2.1 不符清单（行 33-53）
   - 严重程度：严重
   - 改进建议：在 §2.1 新增 3.4.11 行，逐字段列出输入 3 项缺失（`task_items` 子字段 4 项 + `context` + `task_role`）与输出 3 项缺失（`recommended_time` / `summary` / `disclaimer_required`），并标注 P1/P2/P3 三档优先级 vs OOD `Integer priority 1-5` 的语义错位；在 §3 跨章节一致性观察中补充：3.4.11 与 3.4.4 / 3.4.5 / 3.4.6 / 3.4.12 在"数值语义风格"上不一致，需在 Phase 1+ 落实时统一约束。

3. **问题描述**：3.4.5 / 3.4.6 / 3.4.7 输入契约字段遗漏不完整。诊断报告对 3.4.5 仅标"缺 `exam_items`"，遗漏 `exam_id` / `exam_type` / `patient_id` 3 项必填字段；3.4.6 仅标"`rawData: String` vs `lab_items: array`"，遗漏 `lab_id` / `patient_id` 2 项必填字段；3.4.7 仅标"`modality` 代替 `model_id`"，遗漏 `patient_id` 必填字段与 `imageUrl` vs `image_ref` 命名偏差。
   - 所在位置：`a_v1_diag_v1.md` §2.1 行 41-46（3.4.5 / 3.4.6 / 3.4.7 行）
   - 严重程度：严重
   - 改进建议：§2.1 行 41（3.4.5）补全 4 项缺失（`exam_id` / `exam_type` / `patient_id` 必填 + `exam_items` 可选），并标注 `rawData` vs `raw_data_ref` 命名偏差；§2.1 行 43（3.4.6）补全 `lab_id` / `patient_id` 2 项必填缺失，并指出 OOD 完全未引入 `lab_items` 结构（每项含 5 子字段）；§2.1 行 45（3.4.7）补全 `patient_id` 必填缺失，将"`modality` 代替 `model_id`"细化为"`model_id` 是后端岗位推断的模型标识，`modality` 是模态枚举（CT/MRI/X-Ray），语义完全错配"，并指出 `imageUrl` → `image_ref` 的命名偏差。

4. **问题描述**：3.4.10 输出 `PrescriptionDraft` 字段语义偏差未关联标记。OOD `PrescriptionAssistResponse.prescriptionDraft.drugs` 复用 3.4.2 的 `PrescriptionDrug` 类型，字段集偏差已在 §2.1 行 38 标记，但未在 3.4.10 行关联声明，形成"上游已诊断 / 下游未关联"的不一致。
   - 所在位置：`a_v1_diag_v1.md` §2.1 不符清单（3.4.10 未在表内，但 §2.2 行 67 提及 3.4.10 共用 `PatientInfo`）
   - 严重程度：严重
   - 改进建议：在 §2.1 新增 3.4.10 行（或在 §3 跨章节一致性观察中显式标注）："3.4.10 输出 `prescriptionDraft.drugs` 复用 3.4.2 `PrescriptionDrug` 类型，字段集偏差见 §2.1 行 38；需同步修复"，避免修复者只改 3.4.2 入口而忽略 3.4.10 输出。

5. **问题描述**：优先级排序部分倒置。§2.6 Mock 占位规则（影响 13 项 AI 能力 Mock 输出 + Phase 0 前端独立开发依据）与 §2.7 DTO 字段命名风格（全链路契约错位，影响 `packages/shared/types/` 全部类型）影响范围广但仅列为"中"优先级；§2.8 / §2.9（`permitAll` 冗余、profile 切换描述冗余）仅文档表述冗余但同为"中"优先级，分散执行者注意力。
   - 所在位置：`a_v1_diag_v1.md` §2.6 行 162-187、§2.7 行 190-208、§2.8 行 211-223、§2.9 行 226-239
   - 严重程度：一般
   - 改进建议：§2.6 / §2.7 优先级上调为"中偏高"；§2.8 / §2.9 优先级下调为"低"；§4 修复指引方向第 5 项（行 332）将 §2.7 单列为"DTO 字段命名约定（与 §2.1 协同处理，修复成本低）"。

6. **问题描述**：§3.3 完整 JPA 关系映射越界判断需复核。诊断报告将"§3.3 完整 JPA 关系映射"列为越界 Phase 0 范围，但路线图 §0.2 明确"骨架必备"包含"数据与权限模型骨架：数据实体基类与权限模型就位"，骨架与完整映射的边界需澄清。
   - 所在位置：`a_v1_diag_v1.md` §2.3 行 87-109、§4 行 329
   - 严重程度：一般
   - 改进建议：引入路线图 §0.2 "骨架必备" vs §0.4 "明确不包含"对照表，明确骨架必备的实体定义粒度（"实体类 + 主键 + 基本字段 vs 含完整关联映射"）；若 §3.3 完整 JPA 映射超出 Phase 0 范围，将 §3.3 拆分（实体类骨架保留，完整 JPA 映射约定移至 Phase 1 启动前的"模块级接口契约冻结"阶段）；§4 修复指引方向第 2 项需精确化避免误删 Phase 0 必须保留的实体骨架。

7. **问题描述**：§2.1 / §2.2 / §2.3 修复方案潜在副作用提示缺失。DTO 字段对齐会触发连锁变更（Mock 实现层重写、TypeScript 类型同步、前端 UI 分支调整、OpenAPI 规范追溯、需求 vs OOD 优先级分级）；Phase 0 范围回收影响 Phase 1 启动（权限模型落地、统一认证、骨架可演进目标冲突）。
   - 所在位置：`a_v1_diag_v1.md` §2.1 影响范围（行 60-63）、§2.2 影响范围（行 80-81）、§2.3 影响范围（行 107-109）
   - 严重程度：一般
   - 改进建议：§2.1 / §2.2 影响范围补充 5 项连锁变更副作用；§2.3 修复指引方向明确"回收对象 vs 保留对象"清单（实体类骨架与扩展点接口保留，完整 JPA 关联映射与 `@JsonNaming` 实施细节移至 Phase 1 启动前冻结），并补充"回收时同步给出 Phase 1 启动时需要重新冻结的内容清单"；§4 修复指引方向区分"Phase 0 必须对齐的能力"（建议对齐 3.4.1 / 3.4.2 / 3.4.3 / 3.4.12 核心必填字段）与"Phase 1+ 启动前冻结的能力"（其他能力保留 Mock 占位即可）。

8. **问题描述**：§1.4（3.4.10 关联缺失）证据格式与 §1.1 / §1.2 / §1.3 不一致。组件B质询报告指出：§1.4 引用"需求行 993"说明 `prescription_draft.drugs` 字段集，但未在证据核对表中列出 OOD `PrescriptionDraft` 类型的实际行号，与其他关键遗漏项的"双侧行号对照"格式不一致。
   - 所在位置：`a_v1_diag_v1.md` §1.4 事实核对段落
   - 严重程度：轻微
   - 改进建议：§1.4 补充 OOD `PrescriptionDraft` / `prescriptionDraft.drugs` 的具体行号，与 §1.1 / §1.2 / §1.3 形成统一的"需求 §X 行 Y / OOD §X 行 Z"证据格式。

9. **问题描述**：优先级体系存在五档隐含扩展但未显式声明。组件B质询报告指出：§2.1 / §2.2 将 Mock 占位规则与 DTO 命名风格优先级上调为"中偏高"后，报告存在"高 / 中偏高 / 中 / 低 / 极低"五档的隐含扩展，但其他章节未明示该扩展。
   - 所在位置：`a_v1_diag_v1.md` §4 改进建议总结、§6 修复指引
   - 严重程度：轻微
   - 改进建议：在 §4 / §6 优先级排序开头显式声明五档体系（如"高（关键遗漏）→ 中偏高（影响面广）→ 中（局部问题）→ 低（表述修正）→ 极低（可选优化）"），并对 §2.1 / §2.2 的"中偏高"项明确排序位置。

10. **问题描述**：3.4.13 覆盖状态核查遗漏。组件B质询报告指出：§1.1 明确"诊断报告 §2.1 不符清单涵盖 9 项（3.4.1/2/4/5/6/7/8/9/12）"，但未提及 3.4.13 是否覆盖；§7 完成度评估"诊断覆盖率约 70%"遗漏了对 3.4.13 的核查。
    - 所在位置：`a_v1_diag_v1.md` §1 关键遗漏、§7 完成度评估表
    - 严重程度：轻微
    - 改进建议：§1 增加对 3.4.13 覆盖状态的核查（如确认遗漏则补入 §1 关键遗漏清单；如确认已覆盖则在 §7 完成度评估表中明确覆盖率修正值）。

11. **问题描述**：次要问题章节（§5）价值有限，与核心维度关系较弱。组件B质询报告指出：§5.1 §3 重复、§5.2 §4 第 5 项归类、§5.3 §5 范围说明精简、§4.3 §2.8 / §2.9 优先级下调均为文档结构层面的极小修正，与核心维度关系较弱。
    - 所在位置：`a_v1_diag_v1.md` §5 其他次要问题（行 256-285）、§4.3（行 244-254）
    - 严重程度：轻微
    - 改进建议：§5 三个子项与 §4.3 可合并精简为"次要表述问题汇总"一节（共 1 段），或将 §5 整体标注为"可选优化"并明确"以下修正不影响诊断可用性，仅在有余力时处理"；§2.6 枚举风险表述精确化为"Mock 默认填充值为 `EnumType.values()[0]`（即枚举声明顺序的第一个值），若声明顺序与业务中性值不一致则 Mock 默认填充高风险/严重值"；§2.10 FallbackAiService 表述澄清为"OOD §3.4 行 634 声明 FallbackAiService'作为装饰器始终注册为 Bean'，但未显式给出 `@Service` 注解、`implements AiService` 实现关系及 `@Primary` 装饰器标注"；§2.5 修复指引补充"联动修改 §7 决策表对比维度"。


## 迭代第 2 轮

1. **问题描述**：组件 A 诊断报告 §2.1 不符清单针对 "3.4.9 开立检查/检验输出" 行声称 "缺 `disclaimer_required`（固定 true）"，§2.4 第 3 项再次声称 "`ExaminationRecommendResponse`（对应 3.4.9）缺 `disclaimer_required`"，与 OOD §8.2 行 1022-1025 实际定义的 `Boolean disclaimerRequired` 字段矛盾，属事实判断错误。
   - 所在位置：组件 A 产出 `a_v2_diag_v1.md` §2.1 不符清单 "3.4.9" 行、§2.4 第 3 项、§1 概述的 "11 项存在不符点" 计数、§1.1 覆盖率核查表 3.4.9 行。
   - 严重程度：严重
   - 改进建议：删除 §2.1 与 §2.4 中 "缺 `disclaimer_required`" 表述，可改为"`disclaimerRequired` 命名为 camelCase，与需求 snake_case 契约不一致（详见 §2.7 全局命名问题）"，或将该行整体移除；同步核校 §1 概述的 "11 项" 计数与 §1.1 覆盖率表的标注口径。
2. **问题描述**：§1.1 覆盖率核查表 "3.4.13 AI 综合讨论结论" 行标注为 "已覆盖（含 `PatientInfo` 偏差）" 并指向 §2.2 / §2.4，但 §2.2 仅讨论 `PatientInfo` 共用字段语义偏差，§2.4 仅讨论 §8.2 行 907 对齐声明范围，二者均未对 3.4.13 自身字段约束（如 `discussion_text` ≥ 50 字符、`summary` 项数 0–5 项且每项 ≤ 100 字符、`encounter_id` 必填及特殊校验规则）做独立核查；覆盖率 "100%（13 项全部覆盖）" 声明的精确性受损。
   - 所在位置：组件 A 产出 `a_v2_diag_v1.md` §1.1 覆盖率核查表 3.4.13 行、§1.1 覆盖率修正值声明。
   - 严重程度：一般
   - 改进建议：选项 A——在 §2.1 不符清单新增 3.4.13 独立行，逐项核查 `discussion_text` 字符约束、`summary` 项数与项长度约束、`encounter_id` 必填校验等具体偏差；选项 B——保留当前覆盖表但将 3.4.13 标注修正为 "PatientInfo 共享偏差已覆盖；自身字段约束未做独立核查"，并相应调整覆盖率声明为 "字段集核查完整覆盖率 12/13"。
3. **问题描述**：§4 修复指引方向第 2 项将 §2.3（Phase 0 范围越界）标为【高优先级】，第 3 项将 §2.4（§8.2 行 907 对齐声明）标为【高优先级】，第 7 项将 §2.5 / §2.8 / §2.9 聚合为【中优先级】；但 §2.3 / §2.4 章节标题本身标为【中优先级】，§2.8 / §2.9 章节标题标为【低优先级】。§4 与 §2 之间优先级标签矛盾。
   - 所在位置：组件 A 产出 `a_v2_diag_v1.md` §4 修复指引方向条目、§2.3 / §2.4 / §2.8 / §2.9 章节标题。
   - 严重程度：一般
   - 改进建议：选择 §4 作为权威优先级排序，回填到 §2 各章节标题（§2.3 改为【高优先级】、§2.4 改为【高优先级】、§2.8 / §2.9 保持【低优先级】并相应调整 §4 第 7 项表述）；或在 §4 中明确"本条目对应 §2.x 章节的某优先级，因影响范围广建议提前处理"的判断依据。
4. **问题描述**：§2.2 仅分析 OOD `PatientInfo` 的 `allergies: List<String>` 与需求 `allergy_history: string` 偏差，未提及 OOD §8.2 行 941 实际定义的 `List<AllergyDetail> allergyDetails` 结构化字段（包含 `allergen` / `reactionType` / `severity` / `occurredAt` 四子字段，显式声明"缺省时回退到 allergies"），影响修复者对"结构化对象列表 → 中文逗号拼接字符串"映射机制的判断。
   - 所在位置：组件 A 产出 `a_v2_diag_v1.md` §2.2 现象段、§2.1 不符清单 "3.4.4 智能诊断 `PatientInfo`" 行。
   - 严重程度：一般
   - 改进建议：§2.2 现象段补充 `allergyDetails` 结构化字段定义及映射规则说明；§2.2 影响范围或修复建议补充三档修复方案（移除 `allergyDetails` / 同时保留两者并明确序列化映射 / 重命名 `allergies` → `allergy_history` 且类型改为 string 并删除 `allergyDetails`），为修复者提供决策依据。
5. **问题描述**：OOD §8.2 行 936 `PatientInfo` 包含 `String name // 患者姓名` 字段，但需求 §3.4.2 / §3.4.4 / §3.4.9 / §3.4.10 / §3.4.13 的 `patient_info` 契约均未定义 `name` 字段；诊断报告 §2.2 仅讨论字段缺失/语义偏差维度，未提及 "OOD 超出需求的字段" 这一维度，修复者面对"保留/删除/核查其他模块依赖"决策时缺少诊断依据。
   - 所在位置：组件 A 产出 `a_v2_diag_v1.md` §2.2 现象段、不符清单相关行。
   - 严重程度：一般
   - 改进建议：§2.2 不符清单或现象段补充 "OOD `PatientInfo` 包含 `String name` 字段（行 936），但需求 3.4.x 各能力的 `patient_info` 契约均未定义 `name`。修复时需决定保留/删除，并核查其他模块依赖"。

