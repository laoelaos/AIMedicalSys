# 诊断质询报告（v1）

## 质询结果

LOCATED

## 逐维度审查

### 1. 证据充分性

**[通过]** §2.1 逐项不符清单的"需求 §X 行 Y / OOD §X 行 Z"双侧行号对照格式（3.4.1 / 3.4.2 / 3.4.3 / 3.4.5 / 3.4.6 / 3.4.7 / 3.4.8 / 3.4.9 / 3.4.10 / 3.4.11 / 3.4.12）经抽样核对（需求 §3.4.1 行 821-833、§3.4.3 行 857-863、§3.4.4 行 895、§3.4.5 行 917-919、§3.4.10 行 991、§3.4.11 行 1009-1011、§3.4.12 行 1025-1040；OOD §8.2 行 910-918、§8.2 行 924-952、§8.2 行 954-958、§8.2 行 983-1007、§8.2 行 1057-1058、§8.2 行 1071-1082、§8.2 行 1083-1096、§3.4 行 622-629、§4.5 行 770-779）均与原文档逐行一致。

**[通过]** §2.1 中"3.4.3 输出 8 字段结构化 vs OOD `structuredRecord: String` 单字符串"、"3.4.5 / 3.4.6 输出仅 `reportDraft`"、"3.4.7 输入 `modality` vs 需求 `model_id` 语义错配"、"3.4.10 `PrescriptionDraft.drugs` 复用 3.4.2 `PrescriptionDrug` 类型"、"3.4.11 P1/P2/P3 vs OOD `Integer priority 1-5` 语义错位"、"3.4.12 `ScheduleRequest` 平铺 vs 需求 `constraints` 嵌套 + `doctor_pool` 数组"等关键判定均能在需求文档相应章节找到原始定义作为支撑。

**[通过]** §2.2 `PatientInfo.allergies` 字段语义偏差：经核对需求 §3.1.6 行 390 显式声明 `allergy_history` 为 `string` 类型（取 `allergen` 列表以中文逗号拼接），行 393 明确"上述映射规则适用于全部引用 `allergy_history` 与 `comorbidities` 的 AI 能力（3.4.2 / 3.4.4 / 3.4.9 / 3.4.10 / 3.4.13）"。OOD §8.2 行 934-941 的 `PatientInfo` 仅定义 `List<String> allergies` 与 `List<AllergyDetail> allergyDetails`，未定义需求要求的主字段 `allergy_history`（string），诊断结论成立。

**[通过]** §2.3 路线图 §0.4 "明确不包含"越界判断：经核对路线图 §0.2 行 39（"数据实体基类与权限模型就位"）、§0.4 行 69（"模块级接口契约冻结"）、§0.2 行 50（"AI 能力模块 Mock 占位：可跨阶段持续完善，不阻塞 Phase 0 骨架验收"），与 OOD §3.3 行 559-568 完整 JPA 关系映射约定、§8.4 跨模块事件模式示例、§3.5 行 666-681 ApiClient/AuthStore 完整认证基础设施的对照关系成立。"路线图 §0.2 骨架必备 vs OOD 定义粒度对照表"中 7 处越界判定证据充分。

**[通过]** §2.4 §8.2 行 907 对齐范围声明与实际不符：经核对需求 §3.4.9 行 979（`disclaimer_required` 固定 true）与 OOD §8.2 行 1022-1026 `ExaminationRecommendResponse`（确实缺 `disclaimerRequired`），诊断判定成立。

**[通过]** §2.5 "Vite workspace" 表述不准确：经核对 OOD §2.4 行 371（`"workspaces": [...]` 配置位于根 `package.json`，是 npm workspaces 字段而非 Vite 配置）、§7 行 853 设计决策表将 Nx / Turborepo / Vite 并列作为 Monorepo 工具选项，事实判定成立。

**[通过]** §2.6 Mock 占位规则 `EnumType.values()[0]` 语义风险：经核对 OOD §3.4 行 627 原文"枚举字段：填充目标枚举类型的第一个枚举值（`EnumType.values()[0]`）"，且 `audit_result` / `risk_level` / `severity` / `urgency_hint` 等强语义枚举在 §8.2 多处 AI DTO 中出现，诊断判定成立。

**[通过]** §2.7 DTO 字段命名风格（camelCase vs snake_case）：经核对 OOD §8.2 全部 DTO 字段采用 Java camelCase 命名（如 `chiefComplaint` / `patientInfo` / `examType`），且全文未声明 `@JsonProperty` / `@JsonNaming` 等序列化约定；需求 §3.4.x 全部输入/输出字段采用 snake_case 命名（如 `chief_complaint` / `patient_info` / `exam_type`），诊断判定成立。

**[通过]** §2.8 / §2.9 / §2.10 / §2.11 / §2.12 / §2.13 / §2.14 各次要问题位置与原文描述均一致。

### 2. 逻辑完整性

**[通过]** §2.1 从"OOD §8.2 行 907 自陈已对齐 4 项"→"§8.2 表实际列出 13 项 DTO"→"逐项比对需求 3.4.x 字段集"→"根因：§8.2 未声明占位/待对齐边界"→"影响：Phase 2-5 DTO 破坏性调整 + Mock 失效 + 前端 TS 类型漂移 + OpenAPI 规范追溯"形成完整因果链。

**[通过]** §2.2 从"需求 3.1.6 映射规则声明 `allergy_history` 为 string"→"需求 3.4.2 / 3.4.4 / 3.4.9 / 3.4.10 / 3.4.13 共用该字段集"→"OOD `PatientInfo` 用 `List<String> allergies` 代替"→"根因：§8.2 未交叉对照需求 3.4.x 字段集约束"→"影响：5 项 AI 能力 Mock 无法序列化"逻辑完整。

**[通过]** §2.3 从"路线图 §0.4 明确不包含模块级接口契约冻结"→"OOD 多处定义超出骨架必备粒度"→"逐项对照 §0.2 vs OOD 实际定义粒度"→"根因：§1.1'可演进'目标与 §0.4'不冻结接口契约'边界缺乏量化标准"→"影响：Phase 1+ 调整需做破坏性修改 + Phase 0 工期压力 + 3 项副作用"逻辑完整。

**[通过]** §3 跨章节一致性观察中 5 个交叉分析（§3.4 Mock 规则与 §2.1 DTO 缺失的相互作用、§2.2 模块依赖图与 §3.3 权限模型位置声明、§8.2 DTO 与 §3.4 AiService 装配策略的耦合、3.4.2 / 3.4.10 `PrescriptionDrug` 类型复用关联、§8.2 数值语义风格不一致）均为修复时的交叉参考依据，逻辑自洽。

**[通过]** §4 修复指引方向按五档优先级（高 / 中偏高 / 中 / 低 / 极低）组织，且第 1 项明确区分"Phase 0 必须对齐的能力"（3.4.1 / 3.4.2 / 3.4.3 / 3.4.12）与"Phase 1+ 启动前冻结的能力"，与 §2.1 / §2.2 / §2.3 的诊断结论一一对应，未出现逻辑跳跃。

### 3. 覆盖完备性

**[通过]** §1.1 覆盖率核查表逐项列出 13 项 AI 能力的诊断覆盖状态与关键问题位置，对照原始任务需求（13 项能力 3.4.1—3.4.13）覆盖完整。

**[通过]** §2.1 解决了第 1 轮反馈中的 4 项关键遗漏：
- 3.4.3 输入 4 项缺失（`patient_id` / `encounter_id` / `stream` / `dialogue_text` 字符数约束）+ 输出 8 字段 vs `structuredRecord: String` 单字符串结构偏差对照表已补全（§2.1 行 62-63），并标注"流式/非流式架构选型未在 OOD §8.2 中预留"为 Phase 2+ 流式输出阻塞点（§4 第 1 项 3.4.3 子项）；
- 3.4.11 输入 5 项缺失（`task_items` 子字段 4 项 + `context` + `task_role`）+ 输出 4 项缺失（`recommended_time` / `reason` / `summary` / `disclaimer_required`）+ P1/P2/P3 vs `Integer priority 1-5` 语义错位已补全（§2.1 行 76-77），§3 第 5 项补充数值语义风格跨章节观察；
- 3.4.5 / 3.4.6 / 3.4.7 输入必填字段（`exam_id` / `exam_type` / `patient_id` / `lab_id` / `patient_id` / `patient_id`）+ 命名偏差（`rawData` vs `raw_data_ref`、`imageUrl` vs `image_ref`）+ 语义错配（`modality` vs `model_id`）已补全（§2.1 行 65 / 67 / 69）；
- 3.4.10 输出 `PrescriptionDraft.drugs` 复用 3.4.2 `PrescriptionDrug` 类型的关联声明已补全（§2.1 行 75），§3 第 4 项补充类型复用联动分析。

**[通过]** 第 1 轮反馈的 7 项其他改进（优先级五档体系声明、副作用提示分级、范围越界判定复核表、3.4.10 关联标记、证据格式统一、3.4.13 覆盖状态核查、次要问题合并精简）均在 v2 中得到体现。

**[问题-轻微]** §1.1 末尾"覆盖率修正值"统计为"13 项全部覆盖，其中 11 项存在不符点，2 项无不符点"。但 §1.1 表格与 §2.1 详表中 13 项均存在不同程度的不符点（含 `PatientInfo` 共享偏差的 3.4.4 / 3.4.9 / 3.4.13、含 `disclaimer_required` 缺失的 3.4.9）。若将"仅含共享类型偏差 / 单一字段缺失"计为"无不符点"，则 11 / 2 的拆分依据未在 §1.1 中说明，读者可能产生"2 项是 3.4.4 与 3.4.13（共享 PatientInfo）还是 3.4.9（仅 disclaimer_required 缺失）等其他组合"的疑问。该不一致仅影响表述精确性，不影响 §2.1 / §2.2 / §3 中各项诊断结论的可信度。

## 质询要点（CHALLENGED 时存在）

无。
