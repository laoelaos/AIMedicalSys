根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

组件B v1 诊断报告（`b_v1_diag_v1.md`）对 `a_v1_diag_v1.md` 提出 11 项质量问题（组件B质询报告已确认 LOCATED，诊断结论可信），按严重程度归纳如下：

### 关键遗漏（高严重度，4 项）
1. **3.4.3 AI 病历生成 整段遗漏**：`§2.1` 不符清单未涵盖 3.4.3（核心三件套之一）。需补输入 4 项缺失（`patient_id` / `encounter_id` / `stream` / 字符数约束）+ 输出 8 字段退化为单字符串的结构偏差对照表；`§2.4` 补充 3.4.3 实际 OOD 定义与"对齐"承诺的偏差；`§4` 修复指引方向显式纳入 3.4.3，并指出"流式/非流式架构选型未在 OOD §8.2 中预留"是 Phase 2+ 流式输出的阻塞点。
2. **3.4.11 AI 执行顺序推荐 整段遗漏**：需补输入 3 项缺失（`task_items` 子字段 4 项 + `context` + `task_role`）与输出 3 项缺失（`recommended_time` / `summary` / `disclaimer_required`），标注 P1/P2/P3 三档优先级 vs OOD `Integer priority 1-5` 的语义错位；`§3` 跨章节一致性观察补充：3.4.11 与 3.4.4 / 3.4.5 / 3.4.6 / 3.4.12 在"数值语义风格"上不一致，需在 Phase 1+ 落实时统一约束。
3. **3.4.5 / 3.4.6 / 3.4.7 输入契约字段遗漏不完整**：3.4.5 仅标"缺 `exam_items`"，遗漏 `exam_id` / `exam_type` / `patient_id` 3 项必填字段（且 `rawData` vs `raw_data_ref` 命名偏差未列出）；3.4.6 仅标"`rawData: String` vs `lab_items: array`"，遗漏 `lab_id` / `patient_id` 2 项必填字段（且未指出 OOD 完全未引入 `lab_items` 结构契约）；3.4.7 仅标"`modality` 代替 `model_id`"，遗漏 `patient_id` 必填字段与 `imageUrl` vs `image_ref` 命名偏差，且 `modality` 与 `model_id` 语义错配需细化。
4. **3.4.10 输出 `PrescriptionDraft` 字段语义偏差未关联标记**：3.4.10 输出 `prescriptionDraft.drugs` 复用 3.4.2 的 `PrescriptionDrug` 类型，字段集偏差已在 §2.1 行 38 标记，但未在 3.4.10 行关联声明。在 §2.1 新增 3.4.10 行（或在 §3 跨章节一致性观察中显式标注），避免修复者只改 3.4.2 入口而忽略 3.4.10 输出。

### 优先级排序问题（一般严重度，1 项）
5. **优先级排序部分倒置**：`§2.6` Mock 占位规则（影响 13 项 AI 能力 Mock 输出 + Phase 0 前端独立开发依据）与 `§2.7` DTO 字段命名风格（全链路契约错位）影响范围广但仅"中"优先级；`§2.8` / `§2.9`（`permitAll` 冗余、profile 切换描述冗余）仅文档表述冗余但同为"中"。`§2.6` / `§2.7` 优先级上调为"中偏高"；`§2.8` / `§2.9` 优先级下调为"低"；`§4` 修复指引方向第 5 项将 §2.7 单列为"DTO 字段命名约定（与 §2.1 协同处理，修复成本低）"。

### 范围越界判断复核（一般严重度，1 项）
6. **§3.3 完整 JPA 关系映射越界判断需复核**：路线图 §0.2 明确"骨架必备"包含"数据与权限模型骨架：数据实体基类与权限模型就位"。引入路线图 §0.2 vs §0.4 对照表，明确骨架必备的实体定义粒度；若 §3.3 完整 JPA 映射超出 Phase 0 范围，将 §3.3 拆分（实体类骨架保留，完整 JPA 映射约定移至 Phase 1 启动前的"模块级接口契约冻结"阶段）；§4 修复指引方向第 2 项需精确化避免误删 Phase 0 必须保留的实体骨架。

### 副作用提示缺失（一般严重度，1 项）
7. **§2.1 / §2.2 / §2.3 修复方案潜在副作用提示缺失**：DTO 字段对齐会触发 5 项连锁变更（Mock 实现层重写、TypeScript 类型同步、前端 UI 分支调整、OpenAPI 规范追溯、需求 vs OOD 优先级分级）；Phase 0 范围回收影响 Phase 1 启动（权限模型落地、统一认证、骨架可演进目标冲突）。§2.1 / §2.2 影响范围补充 5 项连锁变更；§2.3 修复指引方向明确"回收对象 vs 保留对象"清单；§4 修复指引方向区分"Phase 0 必须对齐的能力"（建议对齐 3.4.1 / 3.4.2 / 3.4.3 / 3.4.12 核心必填字段）与"Phase 1+ 启动前冻结的能力"（其他能力保留 Mock 占位即可）。

### 格式与表述（轻微严重度，4 项）
8. **§1.4（3.4.10 关联缺失）证据格式不一致**：补充 OOD `PrescriptionDraft` / `prescriptionDraft.drugs` 的具体行号，与 §1.1 / §1.2 / §1.3 形成统一的"需求 §X 行 Y / OOD §X 行 Z"证据格式。
9. **优先级体系五档扩展未显式声明**：在 §4 / §6 优先级排序开头显式声明"高（关键遗漏）→ 中偏高（影响面广）→ 中（局部问题）→ 低（表述修正）→ 极低（可选优化）"五档体系。
10. **3.4.13 覆盖状态核查遗漏**：§1 增加对 3.4.13 覆盖状态的核查（确认遗漏则补入 §1 关键遗漏清单；确认已覆盖则在 §7 完成度评估表中明确覆盖率修正值）。
11. **次要问题章节价值有限**：§5.1 §3 重复、§5.2 §4 第 5 项归类、§5.3 §5 范围说明精简、§4.3 §2.8 / §2.9 优先级下调合并精简为"次要表述问题汇总"一节，或将 §5 整体标注为"可选优化"并明确"以下修正不影响诊断可用性，仅在有余力时处理"；§2.6 枚举风险表述精确化为"Mock 默认填充值为 `EnumType.values()[0]`（即枚举声明顺序的第一个值），若声明顺序与业务中性值不一致则 Mock 默认填充高风险/严重值"；§2.10 FallbackAiService 表述澄清为"OOD §3.4 行 634 声明 FallbackAiService'作为装饰器始终注册为 Bean'，但未显式给出 `@Service` 注解、`implements AiService` 实现关系及 `@Primary` 装饰器标注"；§2.5 修复指引补充"联动修改 §7 决策表对比维度"。

## 历史迭代回顾

本任务为第 2 轮迭代，第 1 轮已生成 `iteration_history.md`（11 项问题）。本轮组件B诊断报告（`b_v1_diag_v1.md`）与第 1 轮历史反馈内容高度一致，本轮反馈是第 1 轮的正式化版本（行文更严谨）。分析如下：

- **已解决的问题**：无。本轮未识别出第 1 轮反馈中已被解决的项目（也未单独标注已解决项），全部 11 项问题在本轮反馈中均被重申或深化。
- **持续存在的问题**：全部 11 项问题均为第 1 轮已识别、第 2 轮再次重申的持续性问题。其中 4 项关键遗漏（3.4.3 / 3.4.11 / 3.4.5-7 / 3.4.10 关联）与副作用提示缺失（7）为最优先需解决的项。
- **新发现的问题**：本轮无新增问题类别，但有深化：
  - 关键遗漏 1-3 在第 1 轮反馈中已要求补全，本轮细化到逐字段对照表格式；
  - 副作用提示（7）在第 1 轮反馈中已列出，本轮进一步明确"Phase 0 必须对齐的能力"与"Phase 1+ 冻结的能力"分级建议；
  - 范围越界复核（6）在第 1 轮反馈中已提出，本轮明确"回收对象 vs 保留对象"清单方向。

迭代策略：优先解决 4 项关键遗漏（高严重度），同步调整优先级排序与副作用提示，精确化 §3.3 范围越界判断，最后清理次要表述问题。

## 上一轮产出路径

`Harness\redeliberations\202606160104_ood_phase0_problem_diagnosis\a_v1_diag_v1.md`（共 360 行，未超过 1000 行，无需 COPY_AND_EDIT）

## 用户需求

`Harness\redeliberations\202606160104_ood_phase0_problem_diagnosis\requirement.md`
