# 诊断质询报告（v3）

## 质询结果

LOCATED

## 逐维度审查

### 1. 证据充分性

**[通过]** 关键事实交叉验证通过：OOD §8.2 行 1025 实际定义 `Boolean disclaimerRequired`（A1 修正依据充分），OOD §8.2 行 941 实际定义 `List<AllergyDetail> allergyDetails`（C1 修正依据充分），OOD §8.2 行 936 实际包含 `String name`（C2 修正依据充分），OOD §3.4 行 634 实际声明 FallbackAiService"作为装饰器**始终注册为 Bean**"（D2 修正依据充分）。上述四类关键事实在 OOD 原文中均有对应行号佐证。

**[通过]** 需求侧 3.4.9 / 3.4.13 / 3.4.12 等条款的字段约束、必填语义、错误码定义均与需求文档 §3.4.x 对应行的描述一致（已逐项核对需求原文），§2.1 不符清单中 3.4.13 行（输入校验规则、必填语义、`discussion_text` ≥ 50 字符、`summary` 0-5 项且每项 ≤ 100 字符、`DISCUSS_AI_OUTPUT_INCOMPLETE` 错误码）的偏差判定与需求 §3.4.13 行 1050-1074 的契约文本完全吻合。

**[通过]** 路线图侧 §0.2 "骨架必备" 与 §0.4 "明确不包含" 的边界判定、C3 修正中将 3.4.12 从 "Phase 0 必须对齐的能力" 移除的依据（路线图 §0 将 3.4.1/3.4.2/3.4.3 列为"核心三件套"），均有明确引用。

### 2. 逻辑完整性

**[通过]** 优先级标签内部一致性已修复：§2 章节标题优先级（§2.1—§2.4 高，§2.6/§2.7 中偏高，§2.5/§2.8/§2.9 中，§2.10—§2.14 低）与 §4 修复指引方向的优先级标签（项 1-4 高、项 5-6 中偏高、项 7 中、项 8 低）一一对应，无矛盾（B2 已修正）。

**[通过]** 优先级体系从五档降为四档，§1 概述（行 19 "高 / 中偏高 / 中 / 低 四档"）与 §4 修复指引方向（行 430 "四档体系"）声明一致，§2 全部 14 个子章节中无任何标注为【极低】，体系闭合（B3 已修正）。

**[通过]** A1 修正彻底：3.4.9 `ExaminationRecommendResponse.disclaimerRequired` 已被 OOD 定义却被 v2 误判为缺失——v3 已在 §2.1（不符清单移除 3.4.9 行）、§2.4（第 3 项移除）、§1 概述（"11 项"修正为"12 项（11 项全字段不符 + 1 项部分不符 [3.4.4]）"，从列表中移除 3.4.9）、§1.1 覆盖率表（3.4.9 行关键问题位置更新为 §2.2）四处全部修正，事实判断无残留错误。

**[通过]** 因果链完整：每个 §2.x 子问题均给出"现象 → 根因定位 → 触发条件 → 影响范围"四段式结构，B1（§1.1 覆盖率 100% 声明与 §2.1 实际核查）的不一致通过在 §2.1 新增 3.4.13 独立行（含输入校验规则缺失、必填语义未声明、字符数/项数约束缺失、错误码定义缺失四项偏差）得到闭合。

### 3. 覆盖完备性

**[通过]** 13 项 AI 能力诊断覆盖率自洽：§1.1 覆盖率表与 §2.1 不符清单、§2.2 `PatientInfo` 偏差、§2.4 对齐范围声明均无遗漏，3.4.9 已正确归入"主结构与共享 `PatientInfo` 偏差均在 §2.2 处理"，3.4.13 已正确归入"§2.1（补）/ §2.2"（含自身字段约束与 `PatientInfo` 偏差两个维度）。

**[通过]** C1 / C2 关键遗漏已纳入：§2.2 新增"结构化字段未对齐（`allergyDetails`）"与"超出需求的字段（`name`）"两个独立段，均给出 OOD 行号、需求契约字段集、路线图阶段归属、Mock 占位规则影响四维论证；§3 第 6 项新增联动分析说明 `allergyDetails` 与 `name` 在 Mock 阶段的影响路径；§4 第 1 项 / 第 4 项给出"回收 `name` 字段"与"回收 `allergyDetails` 结构化字段（属 Phase 1+）"的修复指引。

**[通过]** D1 / D2 / D3 可操作性细节已修正：§2.3 "回收对象 vs 保留对象清单" 明确列出"关联表 schema 本身（user_role / user_post / post_function）"在 Phase 0 迁移脚本中不创建、User-Role 关联的骨架表示为 `List<Long> roleIds`（不使用 `@ManyToMany`）；§2.10 第 3 项正确指出 OOD 已明示 FallbackAiService "始终注册为 Bean"（无条件注册，无 `@ConditionalOnProperty`），保留第 1 / 2 项 `@Service` / `implements AiService` / `@Primary` 注解未显式声明的实际缺口；§2.1 修复连锁变更副作用清单从 5 项精简为 4 项，移除与 Phase 0 "骨架" 实际状态不符的"前端 UI 分支"项。

**[通过]** 范围说明完备：§5 明确"本诊断范围内"覆盖 §8.2 DTO 对齐、Phase 0 范围越界、文档内部矛盾、Mock 占位规则；"本诊断范围外"明确后端实现细节、前端 UI 实现细节、AI 模型接入、业务融合逻辑等不属于诊断边界的部分，定位层报告与修复层工作职责边界清晰。

## 质询要点（CHALLENGED 时存在）

无。
